# 复制为 .env.local 后按需修改。.env.local 已在 .gitignore 里（.env*.local），不会被提交。

# .NET 后端的地址，服务端专用（Server Component 直连、代理层转发都用它）——
# 不要加 NEXT_PUBLIC_ 前缀，否则会被打进客户端 bundle，暴露后端真实地址。
# 对应 src/DeepLearning.Api/Properties/launchSettings.json 的 "http (Supabase)" / "http (LocalDocker)" profile。
# 两个 profile 都监听 5255，所以后端切到本地临时数据库时这里不用改。
# 后端当前连的是哪个库：GET http://localhost:5255/health/db（详见根目录 本地开发数据库操作手册.md）。
BACKEND_API_BASE_URL=http://localhost:5255

# Supabase Auth（方案 §4）。两个都要填才会真正启用登录态保护（lib/auth/*.ts、middleware.ts
# 在没配置时会优雅降级成"无需登录"的原型行为，不会崩）。
# URL 和后端 appsettings.Development.json 里的 Supabase:ProjectUrl 是同一个项目，公开信息，
# 不是密钥，先帮你填好；anon key 是唯一一个我们这边确实拿不到、需要你自己去 Supabase 控制台
# （Project Settings → API → anon/public key）复制过来的值，不要用 service_role key。
NEXT_PUBLIC_SUPABASE_URL=https://gznzefrpootzduvyylqr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 登录/注册/忘记密码限流（lib/rate-limit/*）。Upstash 控制台 → 你的数据库 → REST API 那一栏直接
# 复制。缺失时优雅降级为不限流，不会让这三个功能打不通——本地开发不配也没关系。
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 注册页的 Cloudflare Turnstile 人机校验（components/auth/turnstile-widget.tsx、
# app/api/auth-guard/[action]/route.ts）。Cloudflare Dashboard → Turnstile → 你的 site →
# Site Key 是公开的（NEXT_PUBLIC_ 前缀没问题，本来就要打进前端 bundle），Secret Key 只在服务端
# 用，不能加 NEXT_PUBLIC_ 前缀。两个都缺失时优雅降级为跳过校验，本地开发不配也没关系。
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
