# 前端开发进度跟踪

> 基准文档：[翻译练习软件_前端开发方案.md](翻译练习软件_前端开发方案.md)（本仓库）、
> [翻译练习软件_设计文档.md](F:\Projects\DeepLearning\翻译练习软件_设计文档.md)、
> [AGENTS.md](F:\Projects\DeepLearning\AGENTS.md)（后端仓库）。
> 更新时间：2026-08-31（第二次更新）。后端 Step 0-9 已全部完成（258 测试通过），Step 10（生产加固）未开始。
> 本文件只跟踪 **DeepLearning.UI**（前端）的进度。

## 一句话结论

学生端 9 个页面、admin 后台 6 个页面**已全部补齐**，RHF+Zod 表单校验与 Vitest+RTL 测试**已经启动**并有实质覆盖。
剩下**唯一**明确未做的是与后端对接的基础设施（API 客户端/代理层）与鉴权（Supabase Auth）——
按你的要求，这两块留到与后端联调时再做，本文件后面单列一节说明现状与后续对接点。

---

## 二、本轮（第二次）完成的工作

### 1. 补齐 3 个学生端页面
| 路由 | 内容 |
|---|---|
| `/weak-points` | 薄弱点列表 + 状态筛选（`EnumSelect` 复用组件），只读展示 |
| `/progress` | 学习曲线仪表盘：`BandTrendChart`（3 维度 Band 趋势，Recharts LineChart，关键节点用 `ReferenceDot` 高亮）+ `PassRateChart`（通过率 AreaChart）+ AI 趋势点评展示 |
| `/standard-overrides` | 列表 + `/standard-overrides/[overrideId]` 详情页（新增的 `getStandardOverrideById` mock 查询） |

### 2. 完成 `/admin` 内容管理后台 6 个页面
- 新建通用组件 `components/admin/crud-table.tsx`：泛型 CRUD 列表组件，`columns` + `schema`（Zod）+ `fields`（声明式表单字段）+ `onCreate` 作为 props 传入，内部用 React Hook Form + `zodResolver` 做表单状态与校验。5 个"只有 List+Create"的资源页面（exam-types/dimensions/error-taxonomies/prompt-templates/question-bank-categories）全部复用这一个组件，没有重复写表格+弹窗代码。
- 新建 `components/shared/admin-shell.tsx`：admin 专用布局（顶部提示条注明"无角色鉴权保护"，与学生端 `AppShell` 视觉区分），学生端 `AppShell` 加了一个跳转入口（齿轮图标）。
- 6 个页面：
  - `/admin/exam-types`：考试类型 List+Create
  - `/admin/exam-types/[examTypeId]/dimensions`：评分维度，新建即"新版本"，自动关闭上一条仍生效的记录（方案 §3.8 版本化语义已在 mock 层 `createAssessmentDimension` 中实现）
  - `/admin/exam-types/[examTypeId]/error-taxonomies`：错误分类 List+Create
  - `/admin/prompt-templates`：Prompt 模板，examTypeId 与 subjectCategory 二选一（XOR 校验），按 templateType 筛选
  - `/admin/llm-providers`：非 CRUD 的设置页，4 个供应商（claude/openai/deepseek/mimo）的启用切换、thinking/effort 配置、模型目录管理（新增模型 + 切换当前模型），对应 mock 层新增的 `listLlmProviderSettings`/`updateLlmProviderSettings`/`activateLlmProvider`/`listLlmProviderModels`/`addLlmProviderModel`/`selectLlmProviderModel`
  - `/admin/question-bank-categories`：分类 List+Create（支持层级 parentId）+ 独立的"给题目打标签"卡片（`tagQuestionWithCategory`）
  - `/admin/questions/import`：手工导入题目，TaskA/TaskB 切换，`useFieldArray` 管理核心意义点与 TaskB 种子错误列表，复用 `SelectableSourceText` 做种子错误的拖选标注，校验规则镜像后端 `ImportUserQuestionValidator`（见下）
- `/admin` 根路径 `redirect` 到 `/admin/exam-types`。

### 3. 表单校验：React Hook Form + Zod 已启用
新增 `lib/validation/` 目录，四个 schema 文件，逐条镜像开发方案 §11 列出的后端 FluentValidation 规则：
- `submission.ts`：`taskAContentSchema`（非空译文）、`taskBAnnotationSchema`/`taskBContentSchema`（至少一条标注，position/errorCategory/correctedText 校验）、`findOverlappingAnnotations` 工具函数
- `question-generate.ts`：`generateQuestionFormSchema`（seedQuestionIds 最多 5 个、不重复、非空）
- `question-import.ts`：`importUserQuestionSchema`（TaskB 必须有 flawedTranslationText + 至少一条种子错误，position 区间校验 + 不重叠 + 落在文本长度内——这是 AGENTS.md 里点名"最值得参考的校验器"的完整镜像）
- `admin.ts`：5 个 admin 资源的新建表单 schema

**落地范围**：
- 新的 admin 6 个页面的"新建"弹窗表单，全部用 RHF + `zodResolver`（`crud-table.tsx` 内部统一处理）
- `/admin/questions/import` 用 RHF 的 `useFieldArray` 管理动态列表
- 回填进两个**已有**页面：`/practice/[questionId]` 答题提交前用 `taskAContentSchema`/`taskBContentSchema` 校验、`/practice/generate` 出题请求用 `generateQuestionFormSchema` 校验，替换掉原来纯手写的 `.length > 0` 判断

### 4. 测试：Vitest + React Testing Library 已启动
新增 `vitest.config.ts` + `vitest.setup.ts`（含 Radix UI 在 jsdom 下需要的 `hasPointerCapture`/`ResizeObserver` polyfill），`package.json` 加了 `test`/`test:watch` 脚本。**8 个测试文件，62 个用例，全部通过**：
- `lib/band.test.ts`：`bandToColor`/`bandLabel` 的取色与钳位逻辑、`formatDate`
- `lib/types/enums.test.ts`：**用测试钉死"每个枚举值都有对应中文标签"**（方案反复强调的"枚举序号漂移"风险点），以及 Priority.high=0 这个容易写反的排序坑
- `lib/validation/*.test.ts`（4 个文件）：上面 4 个 Zod schema 的正/反例全覆盖，尤其是 TaskB 种子错误的区间重叠/越界校验
- `components/practice/selectable-source-text.test.tsx`：字符偏移选区组件的拖选交互（mouseDown→mouseEnter→mouseUp 得到 `[start, end)`）
- `components/admin/crud-table.test.tsx`：表单校验拦截、成功创建后关闭弹窗、`onCreate` 抛错时正确展示错误且不关闭弹窗

**测试过程中发现并修复了一个真实的可访问性问题**：`crud-table.tsx` 里的 `<Label>` 一开始没有和输入框用 `htmlFor`/`id` 关联，`getByLabelText` 查询失败暴露了这个问题，已修复（所有字段类型统一加上 `id`/`htmlFor`）。

**范围说明**（对齐方案 §13 的测试金字塔，但这次先做了地基）：Vitest 单元测试 + RTL 组件测试已经覆盖了新增的校验层和关键交互组件；MSW（API 层测试）与 Playwright E2E 还没有开始——MSW 目前意义不大（还没有真的 `lib/api/*.ts` 请求层可以 mock），Playwright 通常放在核心链路稳定之后再补，等后端联调阶段一起做更合适。

### 5. 验证方式
本轮所有改动都过了：`tsc --noEmit`（项目开着 `exactOptionalPropertyTypes`/`noUncheckedIndexedAccess` 等严格选项）、`eslint .`、`next build`（20 条路由全部编译成功，其中 14 条静态预渲染、6 条动态路由——`/admin/exam-types/[examTypeId]/dimensions`、`.../error-taxonomies`、`/deep-learning/[questionId]`、`/practice/[questionId]`、`/standard-overrides/[overrideId]`、`/submissions/[submissionId]`）、`vitest run`（62/62），以及起本地 dev server 对所有新页面（含动态路由用真实 mock id）做了一次 HTTP 冒烟检查，均正常返回、无运行时报错。本次更新时（2026-08-31）已重新完整跑过 lint/tsc/vitest/build 四项验证，结论与本节一致，未发现代码漂移。

---

## 三、当前页面/模块完成度总览

| 路由 | 状态 |
|---|---|
| `/`（登录页） | ✅（假登录，见下方"暂缓"说明） |
| `/practice` | ✅ |
| `/practice/generate` | ✅（已接入 Zod 校验） |
| `/practice/[questionId]` | ✅（已接入 Zod 校验） |
| `/submissions/[submissionId]` | ✅ |
| `/deep-learning/[questionId]` | ✅ |
| `/review-library` | ✅ |
| `/weak-points` | ✅ 本轮新增 |
| `/progress` | ✅ 本轮新增 |
| `/standard-overrides` + `/standard-overrides/[overrideId]` | ✅ 本轮新增 |
| `/admin/exam-types` | ✅ 本轮新增 |
| `/admin/exam-types/[examTypeId]/dimensions` | ✅ 本轮新增 |
| `/admin/exam-types/[examTypeId]/error-taxonomies` | ✅ 本轮新增 |
| `/admin/prompt-templates` | ✅ 本轮新增 |
| `/admin/llm-providers` | ✅ 本轮新增 |
| `/admin/question-bank-categories` | ✅ 本轮新增 |
| `/admin/questions/import` | ✅ 本轮新增 |

学生端 9/9、admin 6/6，**页面路由层面已经全部补齐**。

---

## 四、明确暂缓的部分（按你的要求，等后端联调时再做）

### A. 与后端桥接的基础设施（方案 §3-5）
- 没有 `lib/api/` 目录、没有 `app/api/backend/[...path]/route.ts` 代理层、没有 `lib/api/fetcher.ts`、没有 `.env.local.example`
- 所有页面仍然直接 `import ... from "@/lib/mock/store"`，还没有中间层可切换

### B. 鉴权与会话（方案 §4）
- 没有接入 `@supabase/ssr`/`@supabase/supabase-js`，登录页仍是"任意信息 + setTimeout 跳转"的占位实现
- 没有 `middleware.ts`、没有页面级登录态保护——所有页面（含 `/admin`）目前都可匿名访问

**这两块是有意暂缓，不是遗漏**——一旦后端联调开始，建议顺序：
1. 先把 `lib/mock/store.ts` 里每个函数按资源拆到 `lib/api/*.ts`，签名不变，内部换成 `apiFetch` 调用（方案里说的"只换函数体"，现在因为拆开了会更顺）
2. 建代理层 `app/api/backend/[...path]/route.ts`
3. 接 Supabase Auth，登录页换成真实 `signInWithPassword`，`middleware.ts` 加会话刷新
4. `admin` 目前的"仅前端约定保护"（顶部提示条）在此时要考虑要不要收紧（方案 §16 第 4 点已经点名这是已知风险）

---

## 五、次要遗留项（不影响页面完整性，供参考）

- `hooks/use-exam-config.ts` / `hooks/use-current-user.ts`（方案 §8.3 的逻辑复用 hook）还没抽出来，目前各页面仍各自 `useQuery(['categories'], listCategories)` 之类分散调用，或直接 `import { MOCK_USER }`；等接入真实鉴权时一起做更自然，因为 `use-current-user` 到时候要换成读 Supabase session
- MSW（API 层测试）与 Playwright E2E 尚未开始，见上文测试范围说明
- `TemplateType` 枚举的序数顺序是前端按直觉排的，**未与后端 `Domain/Enums` 源码核对**（`lib/types/enums.ts` 里已用注释标注），接后端前务必核实
