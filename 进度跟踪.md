# 前端开发进度跟踪

> 基准文档：[翻译练习软件_前端开发方案.md](翻译练习软件_前端开发方案.md)（本仓库）、
> [翻译练习软件_设计文档.md](../../翻译练习软件_设计文档.md)、[后端进度跟踪.md](../../后端进度跟踪.md)（后端进度，
> 本文下方旧章节里提到的「AGENTS.md 某某一节」指的都是它——那个文件的一级标题仍写着 AGENTS.md，是改名前的遗留）、
> [AGENTS.md](../../AGENTS.md)（后端的协作原则与排查清单，不含进度）。
> 更新时间：**2026-09-09（第十三次更新：视觉重配色（石墨深灰主色 + 海军蓝副色 + 冰蓝灰点缀色，替换靛青/朱砂）+ 新 logo/favicon；批改结果页显示官方通过线 Band、批改/深入学习页 loading 态改成整卡居中 spinner；追问面板新增「Close, skip summary」（不生成结算、不改判）+ AI 起草时的等待态；`/exam-management` 六个管理面板设计翻新——图标化 tooltip 操作按钮、状态徽标统一用 `--success`/`--destructive`/`--warning` token、去掉多处冗余的 Code/Key 列、按业务分组把若干长表拆成多张小表、薄弱点合并操作收进弹窗。见第十七节。前端 66/66 测试、`tsc`、`eslint` 全绿；已提交 git（`67e3bcc`..`18cef05`，6 个提交）**。
> 上一次：2026-09-08（第十二次更新：完整 i18n 双语框架（`src/lib/i18n/`，英文默认 / 中文可切，设置→General 切换）——每一处可见 UI（含 admin/考试管理面板、表单校验、浏览器标签页 metadata）都已抽 key；后端顺带加 `users.language_preference` 列 + `PUT /users/{id}/language-preference` + 修「登录不写 `last_login`」。见第十六节。前端 66/66 测试、`tsc`、`next build`、`eslint` 全绿；已提交 git `9a9c3fb`）。
> 后端 Step 0-9 已完成；2026-09-02 做了 B1–B5 改造轮（见第八节）；**2026-09-03 又做了评判提示词整合 + 错误 `severity`/`summary` + 薄弱点三表重构 + 薄弱点种类后台管理（见 AGENTS.md「2026-09-03」一节，后端 323 测试通过）**。**2026-09-04 / 09-05 又把评判拆成四次调用、并把错误严重度收敛回官方两档（见后端「2026-09-04 / 09-05」一节，411 测试通过）**。**2026-09-05 第二轮：薄弱点种类改成全局共享的两级语言学分类体系（8 一级 + 38 二级）（见后端「2026-09-05：weak-point lifecycle rebuild」一节，475 测试通过）**。**2026-09-06：追问线程分成 knowledge / dispute / score_challenge 三种 kind；score_challenge 能真正改写某维度的 Band（submission → `regraded`）；dispute / score_challenge 结算改成"先出 AI 草稿、用户确认/编辑/重新生成/放弃、再落库"；纯知识追问不再跑结算（见后端「2026-09-06：follow-up threads」一节，495 测试通过；两条迁移 + `reset_followup_prompts_v1_production.sql` 已对真实 Supabase 执行）**。本轮前端改动见下方**第十二节**。 **2026-09-06（深入学习轮）：`deep_learning` 提示词整合成唯一一行、补进原文标题、去掉跨题 `prior_vocab` 段；词汇改成两表（逐题快照 `vocab_expressions` + AI 维护的跨题词条 `vocab_glossary`，后者成为复习库词汇视图的数据源）；标题译文拆成独立字段 `referenceTitle`；供应商 override 前端标签补齐 `vocab_semantic_drift` / `score_challenge_summary`。迁移 `AddVocabGlossaryAndReferenceTitle` + 两条提示词 SQL 已对真实 Supabase 执行，503 测试通过。本轮前端改动见下方**第十三节**。**
> 本文件跟踪 **DeepLearning.Web**（前端，`src/DeepLearning.Web/`）。**2026-09-05 起前端已并入后端仓库统一跟踪**，不再是独立的嵌套 git 仓库、也不再被 gitignore（见第十节末）。

## 一句话结论

**整站已从「mock 数据 + 顶部导航 + 学生端/管理端分离」重构为「真实后端 + 可折叠左侧导航 + 统一外壳」**，并接入了真实 Supabase Auth。
之前「暂缓」的 API 桥接层与鉴权**都已完成**。

---

## 二、与后端的桥接层 + 鉴权（原「暂缓」，现已完成）

| 部分 | 状态 |
|---|---|
| `lib/api/fetcher.ts`（统一 fetcher，`ApiError`） | ✅ |
| `app/api/backend/[...path]/route.ts`（同源代理层，绕开后端无 CORS） | ✅ |
| `lib/api/*.ts`（每个资源一个模块，函数 + React Query 用法），页面全部改用真实接口，不再 `import from lib/mock` | ✅ |
| `.env.local`（`BACKEND_API_BASE_URL` / `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`） | ✅ anon key 已填 |
| `lib/auth/{supabase-client,supabase-server,session}.ts`（`@supabase/ssr`） | ✅ |
| `src/proxy.ts`（会话刷新 + 登录态保护） | ✅ —— **注意：曾误放在项目根 `middleware.ts`，`src/` 项目 Next.js 不执行它，登录保护一直没生效；已 `git mv` 到 `src/`。2026-09-06：Next.js 16 把 `middleware` 约定改名 `proxy`（旧名会告警），已 `git mv src/middleware.ts src/proxy.ts` + 导出函数 `middleware`→`proxy`，`config.matcher` 不变** |
| 登录页真实 `signInWithPassword` + 挂载时会话守卫（已登录访问 `/` 直接进 `/practice`） | ✅ |
| `middleware`：未登录访问受保护路由 → `/`；已登录访问 `/` → `/practice`（重定向时带上刚刷新的 session cookie） | ✅ |

`lib/mock/store.ts` 仍在仓库里但已无页面引用（保留作参考/类型对照）。

---

## 三、整站结构（重构后）

### 外壳
- `app/(app)/` 路由组 + `app/(app)/layout.tsx`：**可折叠左侧导航**（`components/shell/app-sidebar.tsx`，基于 shadcn `sidebar`，`Ctrl/⌘+B` 折叠成图标条）+ **主内容横向铺满不内缩** + `ToastStack` 挂载
- `components/shell/page-shell.tsx`（`PageShell`）：**标题区固定不滚动，滚动只发生在内容区**；`(app)/layout.tsx` 把外层锁成 `h-svh overflow-hidden`。所有页面经 `AppShell`/`AdminShell` 兼容层（已收敛为 `PageShell`）或直接用 `PageShell` 获得该行为
- `components/shell/user-menu.tsx`：nav 底部头像 + 人名 → submenu（个人资料 / 设置 / 退出登录，退出走 `ConfirmDialog`）
- `components/shell/page-header.tsx`（`PageHeader`）：标题 / 描述 / 操作 / 返回按钮
- **不再区分学生端 / 内容管理端**；旧 `admin-shell` 的「无角色鉴权」提示条已移除

### 三个通用弹窗（`components/ui/`）
| 组件 | 说明（需求里的叫法） |
|---|---|
| `SidePanel`（`side-panel.tsx`） | 「popup 右」——右侧铺满视口高度，**非模态**（不锁滚动、外部可选中复制），点外部不关闭，Header/Body/Footer 三段仅 Body 滚动，`width` 可传 |
| `CenterModal`（`center-modal.tsx`） | 「popup 中」——居中带遮罩，`width` 外部传入，`max-h = 视口−4rem`（上下留白），三段式 |
| `ConfirmDialog`（`confirm-dialog.tsx`） | 「确认 popup」——`tone="info"\|"warning"\|"question"` 决定图标/图标色/主按钮色；文案与 `onConfirm`/`onCancel` 调用处传入；`onConfirm` 支持 async（转圈+禁用，成功自动关闭，抛错保持打开） |

`components/ui/toast.tsx`（自定义 `showToast(...)` + `<ToastStack/>`）：已修好 strict-tsconfig 报错并在外壳里挂载。

### 导航项与页面

| nav 项 | 路由 / 交互 | 内容 |
|---|---|---|
| 题库 | `/practice` | `QuestionCard` 加「已练 N 次」徽章（`GET /questions?userId=`）+「记录」按钮 → `QuestionRecordsModal`（`CenterModal`，`GET /submissions?userId=&questionId=`，点进去看当次批改）。右上「AI 出题」按钮 → `AiGeneratePanel` |
| 导入题目 | 侧栏项 → `SidePanel` | `ImportPanelProvider` 挂在 `(app)/layout.tsx`（常驻不卸载）→ 编辑中关掉草稿保留。footer：清空 / 取消 / 导入题目。成功 → toast + 跳答题页 + `clearAll` + `submitted` 一次性锁（下次打开面板才解锁，防重复插入）。旧 `/admin/questions/import` → redirect |
| 考试管理 | `/exam-management` → `/exam-management/[examTypeId]` | 列表页：考试类型表 + description 列 + 每行「配置」按钮，**去掉新建**。配置页：`PageShell` + 返回 + 5 tabs（见下） |
| 复习 | `/review` | `PageShell` + 2 tabs：**复习库**（`ReviewLibraryPanel`，句型/词汇内层 tab）+ **薄弱点**（`WeakPointsPanel`）。旧 `/review-library`、`/weak-points` → redirect |
| 进度 | `/progress` | 原页面，经 `AppShell`→`PageShell` 自动套新壳，无功能改动 |

**AI 出题面板**（`components/practice/ai-generate-panel.tsx` + `hooks/use-ai-generate.ts`，取代 `/practice/generate` 页）：
- 表单：任务类型 / 难度（默认「随机」）/ 题材（默认「随机」）/ 命中薄弱点开关（默认关，开则列出活跃薄弱点）/ 真题种子 checkbox（`GET /questions?isSeedReference=true`，≤5）
- 「随机」= 提交时前端从已加载选项里随机取一个真实值发后端
- 生成中：面板体切换成 loading，footer 禁用
- `useAiGenerate` 状态挂在**不会卸载**的题库页 → 关掉面板不重置、不能重复提交，直到成功
- 成功：`showToast` + 跳答题页 + 重置

**考试配置 6 tabs**（`app/(app)/exam-management/[examTypeId]/exam-type-config-page.tsx` + `components/exam-management/*-panel.tsx`）：
| Tab | 能力 |
|---|---|
| 评分维度 | **TaskA / TaskB 两个表**；行可展开显示 `level_descriptions`（jsonb）转成 `Band N → 原文` 的 key-value 组 |
| 错误分类 | list + create |
| 题库分类 | list + create + **编辑 + 删除**（有子分类/被题目引用 → 后端 409，用 toast 提示）+ 打标签卡片 |
| Prompt 模板 | list（**含停用行**，`GET /prompt-templates` 不传 `isActive` = 全部）+ create + **编辑（含「启用」开关）+ 删除（硬删）** |
| 薄弱点种类 | `GET/POST/PUT /exam-types/{id}/weak-point-catalog`。list（状态徽标：待审·自动 / 待审·手动 / 已启用 / 已退役）+ create + 编辑（状态下拉 = 批准 `proposed→active` / 退役 `→deprecated`；`code` 不可改）+ **合并控件**（表上方两个种类选择器 → `POST .../merge`，把「来源」并入「目标」，来源退役） |
| 标准修正 | 只读列表 + **提升为生效**（observing→active）+ **作废**（`POST /standard-overrides/{id}/deprecate`，`ConfirmDialog`）——不提供编辑/物理删除 |

`components/admin/crud-table.tsx`（`CrudTable`）已扩展（向后兼容）：`onUpdate`+`toFormValues`（行内编辑）、`onDelete`+`deleteConfirm`（`ConfirmDialog`，409→toast）、`renderExpanded`（行展开）、`hideCreate`、`onChanged`。

### 设置 / 个人资料
- `/settings`：`PageShell` + 返回 + tabs（**AI 供应商** = `LlmProvidersPanel`，从原 `admin/llm-providers` 页抽出的纯内容；**通用** = 占位）
- `/profile`：占位页 + 返回，显示当前用户显示名/邮箱

---

## 四、路由总览（重构后，18–19 条）

| 路由 | 状态 |
|---|---|
| `/` | ✅ 登录页（不在 `(app)` 外壳里，无侧栏）；已登录访问会跳 `/practice` |
| `/practice` · `/practice/[questionId]` · `/submissions/[submissionId]` · `/deep-learning/[questionId]` | ✅ 核心练习闭环 |
| `/review` | ✅ 复习库 + 薄弱点 tabs |
| `/progress` | ✅ |
| `/exam-management` · `/exam-management/[examTypeId]` | ✅ 考试管理列表 + 5-tab 配置页 |
| `/settings` · `/profile` | ✅ |
| `/practice/generate` → `/practice` | redirect（AI 出题改 SidePanel） |
| `/admin` · `/admin/exam-types`(+dimensions/error-taxonomies) · `/admin/prompt-templates` · `/admin/question-bank-categories` → `/exam-management`(或对应 `[examTypeId]`) | redirect |
| `/admin/questions/import` → `/practice` · `/admin/llm-providers` → `/settings` | redirect |
| `/review-library` · `/weak-points` → `/review` | redirect |
| `/standard-overrides` → `/exam-management` | redirect |
| `/standard-overrides/[overrideId]` | 仍在（只读详情，不在导航里，可直达，无害） |

6 个孤立的旧 `*-page.tsx` 组件（exam-types / dimensions / error-taxonomies / prompt-templates / question-bank-categories / standard-overrides / review-library / weak-points）已 `git rm`。

---

## 五、消费的后端接口（2026-09-01 新增/扩展，详见 AGENTS.md）

- `GET /questions?userId=` → `myAttemptCount` / `myLatestSubmissionId`
- `GET /questions?isSeedReference=true`（AI 出题种子选择）
- `GET /submissions?userId=&questionId=`（做过的记录）
- `PUT`/`DELETE /question-bank-categories/{id}`（删除有子分类/被引用 → 409）
- `PUT`/`DELETE /prompt-templates/{id}` + `GET /prompt-templates?isActive=`
- `POST /standard-overrides/{id}/deprecate`
- exam-types 列表 + `description` 字段

前端 `lib/api` 与 `lib/types/dtos.ts` 已同步（`QuestionListItem` +2 字段、`SubmissionSummary`、`QuestionBankCategory`/`ExamType` +`description`，`promptTemplateFormSchema` +`isActive`）。

---

## 六、已知坑 / 待办

1. ~~**后端有两条 prompt 模板 SQL 必须手动对 Supabase 执行**~~ —— **已全部应用**。2026-09-02 的 B1–B5 轮把 `fix_grading_dimension_key` / `add_paragraphed_source_text` 跑通；**2026-09-03 轮**的两条迁移（`AddErrorSeverityAndSummary` / `ReworkWeakPointModel`）经 `dotnet ef database update`、两条手写 SQL（`consolidate_grading_prompt_templates.sql` / `fix_weak_point_classification_prompt_add_summaries.sql`）由用户对真实 Supabase 跑通。当前对真实 DB 没有 outstanding 的 schema/模板改动。
2. **服务端 Supabase 会话可能仍不稳**：`src/middleware.ts` 位置修正后登录保护会执行，但如果 middleware 的 `getUser()` / 代理层 `getAccessToken()` 仍拿不到会话（Node 服务端到 Supabase 出站不通、或 `@supabase/ssr` 分片 cookie 未被服务端重组），所有写操作会按 mock 用户（`FALLBACK_USER_ID`）落库、可能因外键失败。排查：做一次真实提交看是否 500 / 后端日志里请求有没有带 `Authorization`。
3. **考试管理里「题库分类 / Prompt 模板 / 标准修正」三个 tab 是全局数据**——后端这三张表没有 `exam_type_id`，在任一考试类型的配置页看到的是同一份。当前只有一个考试类型，无影响。
4. **新增的 panel / popup 没有组件测试**——项目未配 MSW / Playwright；之前的 Vitest 单测（`lib/band`、`lib/types/enums`、`lib/validation/*`、`selectable-source-text`、`crud-table`）仍在。`crud-table.test.tsx` 有 **3 个预存在失败**（jsdom/radix，stash 掉本轮改动后仍失败，与本轮无关）。`weak-point-catalog-panel` / 卡片重新归类下拉未补测。
5. `hooks/use-exam-config.ts` / `hooks/use-current-user.ts` 已存在并被广泛使用（早先「待抽取」的条目已完成）。

---

## 七、验证

每个阶段结束都过了 `npx tsc --noEmit`、`npx eslint .`、`npx next build`（18–19 条路由全部编译，`ƒ Proxy (Middleware)` 存在）。后端 `dotnet test --filter "Category!=LlmIntegration"` = **300 passed**（2026-09-02，B1–B5 轮后）。UI 交互本身未做端到端验证（无 Playwright），需要人工在 `next dev` 里走一遍。

> **2026-09-02 补充**：本轮前端 `tsc` / `eslint .` 均绿。`vitest` 有 **3 个预存在失败**（`src/components/admin/crud-table.test.tsx`，stash 掉所有改动后仍失败，与本轮无关）。本轮改的 5 个前端文件之前混了 LF/CRLF 导致 `prettier` 报错（只跑 `tsc` 没暴露），已 `prettier --write` 归一；顺带修了 `follow-up-panel.tsx` / `side-panel.tsx` 两个**本轮没碰**的既有 lint 欠账，现在 `npx eslint .` 整仓库绿。**本轮 `dtos.ts` / `mock/store.ts` / 三个页面组件有改动，前端需重新 `npm run build` + 重启才生效。**

> **2026-09-03 补充**：本轮前端 `tsc --noEmit` 绿；`vitest` **63 passed / 3 failed**（仍是那 3 个 `crud-table.test.tsx` 既存失败）。改动文件较多（见第九节表），需重新 `npm run build` + 重启。

---

## 八、2026-09-02：后端 B1–B5 改造轮的前端联动

后端做了 5 批改造（AGENTS.md「2026-09-02」一节）。前端只在**批改详情页 / 深度学习页 / 答题页**跟了几处：

| 文件 | 改动 | 对应后端批次 |
|---|---|---|
| `lib/types/dtos.ts` | `SubmissionDetail` 增 `overallSummary: GradingSummary \| null`（新 `GradingSummary` 类型：`overallPassProbability` / `overallPassBool` / `cumulativeDensityFlag/Note` / `conclusionText`）；`VocabExpression` 增 `literalTranslatable: boolean \| null` | B2 / B4 |
| `components/grading/grading-result-panel.tsx` | 「维度评分」卡上方新增区块：**总体预估通过率 xx% · 整体达标/未达标**（`Math.round((p>1?p/100:p)*100)%`，兼容旧行）+ 累积密度提示 | B2 |
| `app/(app)/deep-learning/[questionId]/deep-learning-page.tsx` | ① 词汇卡 `literalTranslatable === false` 时显示「不可机械直译」徽标；② `content` 那个 `useQuery`（`generateDeepLearning`，会触发真实 AI 生成）改为 `retry: 1` + `retryDelay: 3000` + `staleTime: Infinity` + `gcTime: Infinity` + 关掉 focus/reconnect 重取 —— **之前 React Query 默认 `retry: 3` 让一次失败变成 4 请求 × 后端 3 次 = 最多 12 次昂贵调用**（深度学习一直 loading 的元凶之一） | B4 / B4.2 |
| `app/(app)/practice/[questionId]/answer-page.tsx` | 原文卡片 `CardContent` 顶部渲染 `q.title`（英文原文标题，非空时）——之前只在页头显示，且 AI 曾返回中文标题（后端 B1.1 出题模板已修） | B1.1 |
| `lib/mock/store.ts` | 两处 `SubmissionDetail` fixture 补 `overallSummary`；深度学习 mock 的 3 条 vocab 补 `literalTranslatable` | B2 / B4 |

**后端侧对前端可见的其它变化**（前端暂未消费，DTO 里是多出来的 JSON key，TS 接口忽略）：`GetQuestionByIdResult` 多了 `briefDomain/briefTextType/briefPurpose/briefAudience`（`questions.brief` JSONB 仍在，`parseBrief` 展示不变）；出题时后端会写 `question_category_map`（`GetQuestionByIdResult.categoryIds` 早就在读）。

**深度学习延迟**：修好后一次成功生成对 Mimo 要 ~100–135s（贴近后端 180s 总超时）。原文更长可能又超；真到那步后端会考虑给这一个操作单独换快模型。前端 loading 态要能扛住这个时长。

---

## 九、2026-09-03：评判提示词整合 + 错误严重度 + 薄弱点模型重构的前端联动

后端做了三块（AGENTS.md「2026-09-03」一节）：① 评判提示词 4 行合 1、指令去重；② 每条错误加 `severity`(minor/moderate/major/critical) + `summary`；③ 薄弱点三表重构（`weak_points` 去 `description`/`evidence_note`、加 `pattern_summary`，`weak_point_catalog` 加 `status`/`origin`），并加了薄弱点种类后台 CRUD + 合并 + 单条重新归类。前端联动：

| 文件 | 改动 |
|---|---|
| `lib/types/dtos.ts` | `ErrorListItem`：去 `impactsCore: boolean`，加 `severity: number` + `summary: string \| null`。`WeakPoint`：`category`→`label`、`description`→`patternSummary`。新增 `WeakPointCatalogEntry`（id/examTypeId/code/name/description/defaultDimensionKey/defaultErrorCategory/status/origin/createdAt） |
| `lib/types/enums.ts` | 新增 `ErrorSeverity`（`minor:0 moderate:1 major:2 critical:3`）+ `ErrorSeverityLabel`（轻微/中等/较严重/严重）+ `errorImpactLabel(severity)` → `{text, tone}`（**「影响核心/接近边界/非核心」标签由 severity 派生，不是后端单独字段**）；新增 `WeakPointCatalogStatus`（`proposed:0 active:1 deprecated:2`）+ label（待审/已启用/已退役）。`enums.test.ts` 两个新枚举都进了 label 完整性表 |
| `components/grading/grading-result-panel.tsx` | 错误卡片：主徽标改为 `[较严重，术语方向性错误+全文不一致]`（severity + summary，按档位配色）+ 派生的影响核心标签(3 态 3 色) |
| `lib/validation/admin.ts` | 新增 `weakPointCatalogFormSchema`（code 小写下划线、name、description、可选匹配键、status） |
| `lib/api/exam-config.ts` | 新增 `listWeakPointCatalog` / `createWeakPointCatalogEntry` / `updateWeakPointCatalogEntry` / `mergeWeakPointCatalog` |
| `lib/api/weak-points.ts` | 新增 `reclassifyWeakPoint(weakPointId, catalogId)` |
| `components/exam-management/weak-point-catalog-panel.tsx`（新）| 「薄弱点种类」tab 的 CrudTable + `MergeControl`（表上方合并控件），接进 `exam-type-config-page.tsx`（现 6 tabs） |
| `components/weak-points/weak-point-card.tsx` | 用 `label` / `patternSummary`；卡片新增可选「重新归类到…」下拉（`catalogOptions` + `onReclassify` 传入时才显示） |
| `components/review/weak-points-panel.tsx` | 拉 `listExamTypes()`→首个 id→`listWeakPointCatalog`，给每张卡片传 catalog 选项 + `reclassify` mutation（toast 区分「已重新归类」/「已并入已有薄弱点」） |
| `lib/mock/store.ts` | 3 条 `ErrorListItem` fixture 去 `impactsCore` 加 `severity`/`summary`；4 条 weak-point fixture `category`→`label`、`description`→`patternSummary` |

## 十、2026-09-04 / 09-05：评判改为四次调用 + 严重度收敛到官方两档的前端联动

后端这一轮把评卷从**一次 AI 调用改成四次**（证据采集 / 责任校对 / 专项排查 / 定档评卷，前三个并发且互不知道对方的结果，由代码合并），并把自造的四档严重度**收敛回 NAATI 官方的两档**；同时修掉一个 provider 级的 bug——Mimo 默认开启深度思考，思考内容与答案共用 `max_completion_tokens`，且思考开启时 `temperature` / `top_p` 会被强制为 1.0 / 0.95，所以后端为可复现而传的 `temperature: 0` 一直没有生效。详见后端 `后端进度跟踪.md`「2026-09-04 / 09-05」一节（**411 测试通过**）。

**对前端而言，接口形状只变了一处：`ErrorSeverity` 从 4 员变成 2 员。** 其余（`band` / `confidence` / `alternativeBand` / `cumulativeDensityFlag` / `overallSummary`）都是第九节那轮已经接好的字段，本轮没有再动。

| 文件 | 改动 |
|---|---|
| `lib/types/enums.ts` | `ErrorSeverity` 由 `{minor:0, moderate:1, major:2, critical:3}` → **`{minor:0, major:1}`**；`ErrorSeverityLabel` 由「轻微/中等/较严重/严重」→ **「Minor / Major」**（直接用官方术语，不再自造中文档位名）；`errorImpactLabel` 的 `tone` 由三态 `danger｜warning｜muted` → 两态 `danger｜muted`，文案改为「影响理解 / 仅精度损失」——官方 Major 的判据就是「影响意图或功能，或影响读者理解」，所以这个标签与 severity 一一对应，不再有中间态 |
| `components/grading/grading-result-panel.tsx` | `SEVERITY_BADGE` 去掉 moderate / critical 两行；未知 severity 的兜底由 `moderate` 改为 **`minor`**（保守方向：不要把认不出的东西显示得比实际严重）；影响标签的图标/配色由三分支收成两分支，`AlertTriangle` 不再引用 |

### 为什么档位数字变了不需要数据迁移

`error_list.severity` 存的是枚举序数，后端迁移 `CollapseErrorSeverityToNaatiTwoLevels` 已经把历史行一并重映射了。前端只按当前两个值渲染；真遇到超出范围的旧值，上面那条兜底会按 `minor` 显示而不是崩掉。

### 仍然只在后端、前端无需跟进的部分

- 每条错误的两个布尔判据（`q1` 意图与功能 / `q2` 理解结果）**不出现在任何 DTO 里**——它们只用于后端 `DeriveSeverity` 推导 severity，前端拿到的始终是推导后的结果。
- 通过线（`pass_threshold`）同样**从不发给 AI**，通过与否和通过概率都由后端机械计算；前端读的 `passBool` / `estimatedPassProbability` / `overallSummary` 语义没变。
- 四阶段拆分、思考开关、token 预算、重试提示这些全在后端 orchestration 层，接口无感知。**唯一可感知的是耗时**：一次完整评卷仍在分钟级（三个采集阶段并发后约 5–10 分钟），前端的轮询/加载态沿用第九节的实现。

### 一处文档订正

本文件开头写的「**DeepLearning.Web 是一个独立的嵌套 git 仓库，被后端仓库 gitignore**」自 2026-09-05 起**不再成立**——前端已并入后端仓库统一跟踪（185 个文件，`node_modules` 与构建产物由本目录自己的 `.gitignore` 排除）。在后端根目录跑一次 `git` 命令即可同时覆盖前后端，不需要再 `cd src/DeepLearning.Web`。

## 十一、2026-09-05（第二轮）：薄弱点种类改成全局两级分类体系的前端联动

后端把薄弱点种类从"按考试类型各自一份的扁平列表"改成了"全局共享的两级语言学分类体系"（8 个一级分类 × 38 个二级叶子），并重写了薄弱点的状态机（`tracking`/`active`/`resolved`，见后端「2026-09-05：weak-point lifecycle rebuild」一节）。前端只联动了"薄弱点种类"管理面板和薄弱点卡片这两处,评判结果页/答题页无感知。

| 文件 | 改动 |
|---|---|
| `lib/types/enums.ts` | `WeakPointStatus` 新增 `tracking`(**追加在最后 = 2,不是插在中间**——这个枚举没配 `JsonStringEnumConverter`,序列化成裸序数,插在前面会把已有的 `active:0`/`resolved:1` 错位)+ "观察中"标签;`AiOperationType` 新增 `weak_point_detection_criteria`(8)/`weak_point_recheck`(9)+ 中文标签 |
| `lib/types/dtos.ts` | `WeakPointCatalogEntry` 把 `examTypeId` 换成 `categoryId`/`categoryCode`;新增 `WeakPointCategory` 类型(id/code/name/description/displayOrder) |
| `lib/validation/admin.ts` | `weakPointCatalogFormSchema` 新增 `categoryId` 必填校验;顺带修了一个潜藏 bug——`promptTemplateFormSchema.templateType` 的上界还停在旧的 7,没跟着新枚举值放宽到 9,新建/编辑 `weak_point_detection_criteria`/`weak_point_recheck` 这两类模板时会被 zodResolver 静默拦下 |
| `lib/api/exam-config.ts` | 薄弱点种类相关的 4 个 API 函数(`listWeakPointCatalog`/`createWeakPointCatalogEntry`/`updateWeakPointCatalogEntry`/`mergeWeakPointCatalog`)去掉 `examTypeId` 参数(后端路由从 `/exam-types/{id}/weak-point-catalog` 改成了全局的 `/weak-point-catalog`);新增 `listWeakPointCategories()` 拉一级分类列表 |
| `components/exam-management/weak-point-catalog-panel.tsx` | 重写:不再需要 `examTypeId` prop;新建表单加"一级分类"下拉(拉 `listWeakPointCategories`);表格加一列显示所属一级分类;合并控件(`MergeControl`)同步去掉 `examTypeId` |
| `components/review/weak-points-panel.tsx` | 简化:薄弱点种类现在全局共享,不用再先查 `listExamTypes()` 拿到考试类型 id 才能查 catalog,直接 `listWeakPointCatalog()` |
| `app/(app)/exam-management/[examTypeId]/exam-type-config-page.tsx` | 传给 `WeakPointCatalogPanel` 的 `examTypeId` prop 去掉 |

**验证**:`npx tsc --noEmit` 全项目类型检查通过;`vitest run` 跑过,3 个失败全部确认是 `crud-table.test.tsx` 里与本轮无关的预置失败(对比改动前后一致)。UI 交互本身未做端到端验证,需要人工在 `next dev` 里走一遍薄弱点种类管理页。

## 十二、2026-09-06：追问线程大改（kind 分线 + 维度改判 + 结算前人工确认）

后端把追问线程分成三种 `kind`（`knowledge` 纯问知识 / `dispute` 质疑评判 / `score_challenge` 申请改分），`score_challenge` 结算时能真正改写某维度的 Band 并把 submission 置为新状态 `regraded`；dispute / score_challenge 的"结束"改成两步：先 `POST …/close/preview` 出一份 AI 草稿（线程不关、不落库），前端展示给用户确认 / 编辑 / 重新生成 / 放弃，确认后再 `POST …/close` 带上（可能改过的）结果落库、且不再跑 AI；纯 knowledge 线程直接关闭、不跑结算。详见后端「2026-09-06：follow-up threads」一节（**495 测试通过**）。

| 文件 | 改动 |
|---|---|
| `lib/types/enums.ts` | 新增 `FollowUpThreadKind`(knowledge:0/dispute:1/score_challenge:2)+ 标签、`ScoreChallengeDecision`(字符串枚举 "uphold"/"adjust")；`SubmissionStatus` **末尾**追加 `regraded:9`(=后端也追加在末尾，别插中间——裸序数会错位)+ "已改判"标签；`AiOperationType` 追加 `score_challenge_summary:10` + 标签 |
| `lib/types/dtos.ts` | `CreateFollowUpThreadRequest` 加 `kind?`/`dimensionId?`；`FollowUpThreadSummary` 加 `kind`/`dimensionId`；`FollowUpThreadDetail` 加 `kind`/`dimensionId`；`GradingResultItem` 加 `dimensionId`(改判入口要用它锚定维度)；新增 `FollowUpClosePreview`/`FollowUpCloseInput`/`StandardRevisionDraft` |
| `lib/api/follow-up-threads.ts` | 新增 `previewFollowUpClose(threadId,userId)`；`closeFollowUpThread` 加可选 `input` 参数(带上则后端直接落库不跑 AI) |
| `components/grading/follow-up-panel.tsx` | 大改：① 新增 `scoreChallenge` / `disputeAnchor` / `renderTrigger` 三个 prop——分别对应"从分数区申请改判"、"从错误清单引用某条质疑"、"自定义触发器"；② dispute/score_challenge 的"结束"走 preview → 一张可编辑的结算草稿卡片(结论说明 textarea / 最终结论或裁决按钮组 / user_correct 时可编辑 standardRevision / adjust 时可填 revisedBand+依据)→ 确认并结束 / 重新生成 / 放弃；knowledge 线程仍是直接"确认结束"；③ 默认触发按钮在有进行中线程时变成实心的"查看进行中的追问 / 改判申请"(线程列表 query 提前到未打开也拉)；④ 陈旧 `active` 守卫 + `detail` 查询 404 时给可恢复的提示，避免跨 submission 导航后线程 id 变野导致面板卡死 |
| `components/grading/grading-result-panel.tsx` | 拉本 submission 的线程列表；错误清单每条下面加"针对这条错误提问 / 质疑"(设 `contextRef`，后端据此走 dispute)；每个维度行下面加"对这个维度的 Band 申请改判"(有进行中改判申请时变成"查看进行中的改判申请")；这些内嵌 `FollowUpPanel` **始终挂载**、只有可点入口按 `renderTrigger` 条件显隐——修掉了"发送第一条消息建线程后 submission 转 `under_dispute`、条件翻转、面板被卸载、popup 自动关闭"的偶发 bug |
| `app/(app)/submissions/[submissionId]/submission-page.tsx` | `graded` 判定加进 `regraded`(结果区 / 追问按钮在改判后仍显示)；主 `FollowUpPanel` 加 `key={submissionId}`——跨 submission 导航时彻底重置，不再把上一条 submission 的 `active` 线程 id 带过来 |
| `lib/mock/store.ts` | mock 的 3 条 `GradingResultItem` 补 `dimensionId` 字段 |

**验证**:`npx tsc --noEmit` 全项目类型检查通过;`npx eslint` 本轮改的文件全绿;`vitest run` **63 passed / 3 failed**(仍是那 3 个 `crud-table.test.tsx` 既存失败)。UI 交互未做端到端验证——需要人工在 `next dev` 里走一遍：错误清单引用提问、分数区申请改判(uphold / adjust 两条路径)、结算草稿的编辑与重新生成、跨 submission 导航后回来线程还能用。**本轮 `enums.ts` / `dtos.ts` / `mock/store.ts` 有改动，前端需重新 `npm run build` + 重启才生效；且需要新的后端代码一起部署(旧后端能容忍新库 schema，但模板行为不会按新设计生效)。**

## 十三、2026-09-06（深入学习轮）：提示词整合 + 词汇两表模型 + 标题译文拆字段 + override 标签补全

后端把 `deep_learning` 提示词从「5 行 / 2 行 active（一段 NAATI 定位碎片 + `shared_methodology` v4 正文，被 `ExamConfigLoader` 拼接下发）」整合成唯一一行 `exam_specific` v1；去掉了塞进主生成 prompt 的跨题 `prior_vocab` 去重段，改由后端两张表承担：`vocab_expressions` 保持逐题快照（生成即冻结），新表 `vocab_glossary` 每个 `canonical_key` 一行、由新的后台 AI 调用 `vocab_semantic_drift` 维护「累积语义」——词首次出现确定性写种子，之后每次在别的题里复现才判断是否有新义、有才丰富。`vocab_glossary` 成为**复习库词汇视图**的数据源。另外补进了原文标题（`questions.title`），并把标题译文从 `referenceText` 首行拆成独立字段 `referenceTitle`（新增 `reference_translations.reference_title` 列）。详见后端「2026-09-06：deep-learning prompt reset + vocab_glossary two-table model」一节（**503 测试通过**；迁移 `AddVocabGlossaryAndReferenceTitle` + `reset_deep_learning_prompt_v1_exam_specific.sql` + `add_vocab_semantic_drift_prompt_template.sql` 已对真实 Supabase 执行）。

| 文件 | 改动 |
|---|---|
| `lib/types/dtos.ts` | `DeepLearningContent` 加 `referenceTitle: string \| null`（标题译文，与正文 `referenceText` 分开）；`ReviewVocabItem` 去掉 `questionId`、加 `accumulatedSemantics` / `category` / `senseCount` / `occurrenceCount`（复习库词汇改读 `vocab_glossary` 词条，一个词一条、带累积多义）；`AiOperationType` 联合类型补 `"score_challenge_summary"` / `"vocab_semantic_drift"`（注释「固定 10 行」→ 12 行） |
| `lib/types/enums.ts` | `AiOperationType` 常量补 `vocab_semantic_drift: 11`；`AiOperationTypeLabel` 补「词汇语义积累」 |
| `app/(app)/admin/llm-providers/llm-providers-page.tsx` | `OPERATION_TYPE_LABEL` 补「改判申请结算」`score_challenge_summary` + 「词汇跨题语义积累」`vocab_semantic_drift`——之前这两行在后台「LLM 供应商」页 override 列表里标签是空的（行本身有、provider 下拉能用，就是看不出是哪个任务） |
| `app/(app)/deep-learning/[questionId]/deep-learning-page.tsx` | 参考译文卡片顶部：`referenceTitle` 存在时单独一行加粗渲染，再接 `referenceText` 正文 |
| `components/review/review-library-panel.tsx` | 词汇卡片改为「词条（`englishExpr` / `chineseEquiv`）+ 累积语义（`accumulatedSemantics`，可含多义、`whitespace-pre-line`）+『N 个义项 · 出现 M 次』」；不再有「回到原题」链接（词条跨题、无单一 `questionId`）；筛选 badge 用 `category ?? frequencyTag` |
| `components/review/use-review-library.ts` | 无需改（泛型，`domain` 并集两侧都有） |
| `lib/mock/store.ts` | `reviewVocab` 5 条重写：去 `questionId`，补 `accumulatedSemantics` / `category` / `senseCount` / `occurrenceCount`（含一条「instalment」有两个义项的示例）；`getDeepLearningContentByQuestionId` 的 mock content 补 `referenceTitle` |

**验证**:`npx tsc --noEmit` 通过;`npx eslint`（改动文件）全绿。UI 交互未做端到端验证——需人工在 `next dev` 里核对：深入学习页标题译文单独成行、复习库词汇卡片展示累积语义与出现次数、后台「LLM 供应商」页能看到「词汇跨题语义积累」并单独绑 provider。**本轮 `dtos.ts` / `enums.ts` / `mock/store.ts` 有改动，前端需重新 `npm run build` + 重启;且需与新后端代码一起部署（`ReviewVocabItem` / `DeepLearningContent` DTO 形状已变，旧后端返回的 JSON 会缺字段）。**

## 十四、2026-09-06：`progress_trend` 提示词重置——前端零改动

只改后端提示词。`reset_progress_trend_prompt_v1_exam_specific.sql`:`DELETE FROM prompt_templates WHERE template_type='progress_trend'`(删掉旧的 `shared_methodology`/`subject_category='translation'` 行)+ 插入唯一一行 `exam_specific` v1(`exam_type_id='11111111-…-1111'`、`subject_category=NULL`),与本日 weak-point / follow-up / deep-learning 三次重置同一形状。模板变量不变(`difficulty_tier` / `current.*` / `history[]`),后端 `GenerateProgressTrendSnapshotCommandHandler` 不动、无迁移。内容改动:写死 NAATI CT Task-A 三维度各自的官方通过线(`meaning_transfer` / `language_proficiency` 达标线 Band 2、`textual_norms` Band 3,三者须同时达标才整体通过);`trendNote` 改为按「达标 / 临界 / 未达标」逐维度点评 + 点名变化量 + 给最弱维度一条可执行建议;`keyTurningPoint` 收窄成「首次跨过/跌破通过线 / 连续 ≥2 周期下滑后止跌 / 历史最佳最差 / 通过率首次触达或跌离 100%」这类事件(±0.3 Band 且 ±10pp 内视为正常波动)。已用 `dotnet run -- sql apply` 对真实 Supabase 执行(`sql status` pending 1 → 0)。详见后端「2026-09-06:progress_trend prompt reset — shared → exam_specific」一节。

**前端影响:无。** `/progress`(「学习曲线」页)、`band-trend-chart.tsx`、`pass-rate-chart.tsx`、`lib/api/progress.ts`、`ProgressSnapshot` DTO 全部不变——`trendNote` / `keyTurningPoint` 的字段形状没动,只是生成它们的提示词换了。无需重新部署前端。

**顺带(同日,与提示词无关):** `next dev` 报 `The "middleware" file convention is deprecated. Please use "proxy" instead.`(Next.js 16)。已 `git mv src/middleware.ts src/proxy.ts`、导出函数 `middleware` → `proxy`,`config.matcher` 与全部逻辑不变;顺手把 `supabase-server.ts` / `login-page.tsx` 注释里的「middleware」措辞改为「proxy」。`npx tsc --noEmit` 通过。

> 曾提过一个更大的进度分析改造(按需 `GET /api/v1/progress/timeline`,支持 天/周/月(默认)/自定义 粒度、图上画各维度通过线、修 `pass-rate-chart.tsx` 把已是 0–100 的 `passRate` 又 ×100 的 bug、修 `band-trend-chart.tsx` 把日行/周行混画的 bug),**用户看过差距分析后决定不做**,仅保留提示词这一项。若日后重拾,后端「2026-09-06:progress_trend prompt reset」一节的「Scope not taken」小节记录了完整方案。

## 十五、2026-09-06：全站界面文案改英文 + 侧栏 logo 更换（纯展示层，已提交 git `75bc4b7`）

用户手动把面向用户的界面文案由中文改为英文，并换掉侧栏品牌标识。**纯前端展示层——无接口 / DTO / 后端改动，无需与后端一起部署**。已提交为 `75bc4b7`「refactor: update titles and labels to English across the application」（`master` 领先 `origin` 1 个提交），本轮所有改动都在这一个提交里，工作树干净。

| 文件 | 改动 |
|---|---|
| `src/app/layout.tsx` | `metadata.title` `译练` → `Deep Learning`（`template` 同步 `%s · Deep Learning`）；`description` 改成英文一句话 |
| `src/lib/types/enums.ts`（约 122 行改动） | 所有 `*Label` 常量的值由中文改英文：`TaskTypeLabel`（去掉「· 翻译 / · 找错标注」后缀）、`DifficultyLabel`、`SubmissionStatusLabel`、`FollowUpVerdictLabel`、`OverrideStatusLabel`、`WeakPointCatalogStatusLabel`、`WeakPointStatusLabel`、`PriorityLabel`、`MasteryLevelLabel`、`SubjectCategoryLabel`、`TemplateLayerLabel`、`CheckpointImportanceLabel`、`ErrorSeverityLabel`、`errorImpactLabel` 文案等。**枚举的键与数值未动，只有展示字符串变**——`enums.test.ts` 的 label 完整性表按键校验，不受影响 |
| `src/components/shell/nav-config.ts` | 4 个导航项 label：题库 → `Question Bank`、考试管理 → `Management`、复习 → `Review`、进度 → `Progress` |
| `src/components/shell/user-menu.tsx` · `page-header.tsx` · `components/practice/question-card.tsx` · `components/review/weak-points-panel.tsx` | 内联中文文案（菜单项 / 按钮 / 占位符 / 描述）改英文 |
| 各页面 `page.tsx`（practice / review / progress / profile / settings / exam-management + `[examTypeId]` + `app/page.tsx`）与 `*-page.tsx`（practice-page / progress-page / review-page / exam-management-page / exam-type-config-page） | 页头标题 / metadata / 标签 / 占位符 / 描述文案改英文 |
| `src/components/shell/app-sidebar.tsx` | 去掉 `译练` 衬线字样 + `GraduationCap` 图标框，换成新增的 `public/logo.svg`（深色圆角方块 + 双对勾，**无文字**）。折叠态（`useSidebar().state === "collapsed"`）下 logo 槽位变成展开按钮：默认显示 logo，`group-hover/logo` 时切换成 `PanelLeft` 图标，移开鼠标恢复，点击调 `toggleSidebar()`；展开态仍是 logo 链接到 `/practice` + 旁边独立的 `SidebarTrigger` |
| `public/logo.svg`（新）· `public/favicon.ico`（换新） | 品牌资源 |

**验证**：本轮为用户手动改动并自行提交；文案替换不涉及类型 / 逻辑。若要本地确认，`npx tsc --noEmit` + `npx next build` 应仍绿（未新增依赖 / 未改结构）。UI 需重新 `npm run build` + 重启查看新文案与 logo。

## 十六、2026-09-08：完整 i18n 双语框架（英文默认 / 中文可切）+ 后端 `language_preference` 列 + last_login 修复

第十五节把界面写死成英文；本轮引入真正的 i18n 机制，让用户能在 **设置 → General** 里切中/英，默认英文，登录用户的选择落库跨设备生效。**大部分是前端**；后端只在 `users` 表加一列 + 一个写接口 + 修一个 last_login bug（详见 `后端进度跟踪.md`「2026-09-08」一节）。**已提交 git `9a9c3fb`。**

### i18n 机制（`src/lib/i18n/`，全新）

| 文件 | 作用 |
|---|---|
| `config.ts` | `LOCALES = ["en","zh"]`、`DEFAULT_LOCALE = "en"`、`LOCALE_STORAGE_KEY = "yilian.locale"`、`LOCALE_LABELS`（`zh: "中文"` —— 语言名用自身语言写，故意保留中文）、`isLocale`/`normalizeLocale` |
| `messages/en.ts` | **真相源**：所有 key 在这里定义（`~470` 条），导出 `MessageKey` 联合类型。区域前缀分组（`common.*` / `nav.*` / `login.*` / `practice.*` / `review.*` / `progress.*` / `deepLearning.*` / `answer.*` / `submission.*` / `grading.*` / `followUp.*` / `aiGen.*` / `import.*` / `crud.*` / `examMgmt.*` / `llm.*` / `overrideDetail.*` / `v.*`(zod) / `meta.*`(SSR) …） |
| `messages/zh.ts` | `Record<MessageKey, string>` —— 缺 key 直接编译报错 |
| `index.tsx` | `I18nProvider` / `useI18n()` / `useT()` / `tFormError()`。优先级：**已登录 `users.language_preference` > `localStorage` > `"en"`**。`setLocale` 立即写 localStorage + 已登录时 `PUT` 落库并 invalidate `["current-user"]`；`[locale]` effect 同步 `document.documentElement.lang` + 一份 cookie（仅供 SSR 读，见下）。`I18nProvider` 挂在 `providers.tsx` **内层**（`QueryClientProvider` 之内，因为用了 `useCurrentUser`） |
| `enum-labels.ts` | `useEnumLabels()`（`locale==="zh"` 时返回中文映射，否则用 `enums.ts` 里的英文常量）、`useErrorImpactLabel()`、`useBandLabel()`。`enums.ts` 的 `*Label` 英文常量保持为真相源，中文另存一份在这里 |
| `server.ts` | `getServerLocale()`（读 `yilian.locale` cookie，`next/headers`）+ `serverT(locale)` —— 仅 `generateMetadata` 用 |

### 前端接入 + 数据流

| 文件 | 改动 |
|---|---|
| `app/providers.tsx` | 包一层 `<I18nProvider>` |
| `lib/types/dtos.ts` · `hooks/use-current-user.ts` · `lib/mock/store.ts` | `UserProfile` / `CurrentUser` / MOCK_USER 加 `languagePreference`；`use-current-user` 从 `GET /users/{id}` 读它 |
| `lib/api/users.ts` | 新增 `updateUserLanguagePreference(id, locale)` → `PUT /users/{id}/language-preference` |
| `app/(app)/settings/page.tsx` | 「通用」tab 从占位变成**语言选择器**（`Select`，`LOCALE_LABELS`），切换即 `setLocale` + toast。tab 顺序调成 General 在前 |
| `hooks/use-current-user.ts` | `FALLBACK_USER.displayName` 「练习者」→ `Learner`（原型/未登录态会显示） |

### 已全部抽 key 的界面（`t()` + en/zh 两版，切换即时生效）

外壳（`nav-config` 用 `labelKey`、`app-sidebar`、`user-menu`、`page-header` 的「Back」）、登录页、设置、个人资料、404 / error 页、题库列表、`question-card`、`difficulty-badge`、`ai-loading-state`（补了 `"use client"`）、`enum-select`、`ui/{confirm-dialog,center-modal,side-panel}`（close/cancel/ok 默认值）、复习页（+ `review-library-panel` / `weak-points-panel` / `weak-point-card`）、进度页（+ `band-trend-chart` / `pass-rate-chart` 的图例）、`question-records-modal`、深入学习页、答题页、批改结果页、`grading-result-panel`、**`follow-up-panel`（最大的一个，744 行）**、`ai-generate-panel`、`import-question-panel`、`crud-table`（默认 prop 文案 + toast）、考试管理落地页 + 配置页。

**考试管理 6 个面板 + admin/供应商 + 标准修正详情**也全部抽 key（`examMgmt.*` / `llm.*` / `overrideDetail.*`）：模块级的 `columns`/`fields` 数组移进组件、或改成 `buildColumns(t, …)` / `buildFields(t, …)` 工厂（吃 `TranslateFn` + `EnumLabels` 切片）；`render:(t)=>` 的参数统一改名 `row` 避免遮蔽翻译函数；`llm-providers-page` 的 `OPERATION_TYPE_LABEL` map 改成 `opTypeLabel(t, op)`。

**表单校验（zod）**：`validation/{admin,question-generate,submission,question-import}.ts` 里的自定义消息改成 `v.*` i18n key（带参的编码成 `v.key::<值>`，目前只有 `rangeWithinLength::{len}`）。新 `tFormError(t, message)`（`i18n/index.tsx`）：`v.` 开头的翻译、其余（zod 内建提示）原样返回；在两个展示点用上——`crud-table.tsx`（字段错误 + handleInvalid toast）和 `import-question-panel.tsx`。schema 的导出与签名没变，所以消费方 + 4 个校验测试文件（只断言 `.success`）都不用动。

**浏览器标签页 / OG（metadata）**：6 个叶子 `page.tsx`（deep-learning/[id]、practice/[id]、submissions/[id]、standard-overrides/[id]、progress、根 `page.tsx`）从静态 `metadata` 常量改成 `generateMetadata`，用 `serverT` + `meta.*` key。**根 `layout.tsx` 故意保持静态英文**（在那里读 cookie 会让整站退化成按需渲染）——`I18nProvider` hydration 后会把 `<html lang>` 改对。净效果：`/` 和 `/progress` 变成 `ƒ` 动态，其余仍是 `○` 静态预渲染。

### 后端（`后端进度跟踪.md`「2026-09-08」一节，迁移已由用户对真实 Supabase 执行）

- `User.LanguagePreference`（`string`，默认 `"en"`）+ `UserConfiguration` 映射 `language_preference varchar(8) NOT NULL default 'en'` + 迁移 `20260908021003_AddUserLanguagePreference`（纯 add，NOT NULL + 默认值 → 存量行回填 `en`）。
- `GetUserByIdResult` + handler 带上 `LanguagePreference`；新 feature 文件夹 `Features/Users/Commands/UpdateUserLanguagePreference/`（Command/Handler/Result/Validator，只允许 `en`/`zh`）；`UsersController` 加 `PUT api/v1/users/{id}/language-preference`（注入 `ICurrentUserService`，`_currentUser.UserId ?? id`）。
- `EnsureUserProfileMiddleware`：建号时也写 `LastLoginAt = now`；已存在的行**每 UTC 日最多**把 `LastLoginAt` 刷一次（修「登录从不更新 `last_login`」，且不让每个 API 调用都 UPDATE `users`）。

### 保留不译（有意）

`lib/mock/store.ts` 的演示内容（原型模式才用的模拟"已存储数据"，用户明确说存储的中文不用管）；`answer-page` / `import-question-panel` 里 `brief` 的 JSON key（`领域`/`文本类型`/`目的`/`受众` —— 后端 `brief` jsonb 的键，`parseBrief` 按它匹配，从不直接显示）；`deep-learning-page` 的 `UNCATEGORIZED = "其他"`（内部分组哨兵，渲染时过 `t()`）；代码注释。

### 验证

`npx tsc --noEmit` 全绿；`npm run test` **66/66 通过**（`crud-table.test.tsx` 本轮**重写**：因为 `CrudTable` 现在用 `useT()`，测试包了 `QueryClientProvider` + `I18nProvider`，断言改英文串——它在 `master` 上本来就 3/4 挂，现在 4/4 绿）；`npx next build` 编译通过；`npm run lint` **完全干净**（顺带删掉了 `app-sidebar.tsx` 里两条引用未注册规则 `@next/next/no-img-element` 的 `eslint-disable` 死注释——那是本仓库 flat config 从未装 Next.js eslint 插件留下的报错，与 i18n 无关）。**本轮 `dtos.ts` / `enums.ts`（无实际值改动，仅 `enum-labels.ts` 引用）/ `mock/store.ts` / 大量组件有改动，前端需重新 `npm run build` + 重启；且需与新后端代码一起部署（`GET /users/{id}` 现在返回 `languagePreference`，`PUT …/language-preference` 是新路由，旧后端没有）。迁移已应用到 Supabase，未应用前 `GET /users/{id}` 会因缺列 500，但 `use-current-user` 有 catch 兜底、界面仍可用（只是不落库语言偏好）。**

## 十七、2026-09-09：视觉重配色 + 批改/深入学习页 loading 与通过线展示 + 追问「跳过结算」+ `/exam-management` 六面板设计翻新

纯展示层 + 一个前端触发的后端新可选参数（`SkipSummary`，见 `后端进度跟踪.md`「2026-09-09」一节），**无 schema/接口破坏性变更**。分 6 个提交（`67e3bcc`→`4881ae8`→`f0eae0c`→`93e7196`→`417d979`→`18cef05`），**均已提交 git**。前端 `npm run test` 66/66、`tsc --noEmit`、`eslint .`（本轮结束前跑了一次 `--fix`，清掉了 3 个残留的 prettier 格式问题，其中 2 个是更早提交遗留、与本轮无关）全绿。

### 视觉重配色 + 品牌资产（`67e3bcc`）

`globals.css` 的语义色 token 整组重调：主色由「靛青」换成「石墨深灰」（`--primary`），新增「海军蓝」副色（`--secondary`，原来的浅米色底纯装饰色被换掉）、强调色从「朱砂」换成「冰蓝灰」（`--accent`）——文件顶部的设计说明注释同步改写。新增 `public/logo-dark.svg`（深色主题用），`public/logo.svg` 本身也调整；`favicon.ico` 换新。`question-card.tsx` 同轮做了一次布局细化（难度徽标改用 `text-success`/`text-warning-foreground`/`text-destructive` 语义 token，而不是硬编码色——后续几轮的状态配色都沿用这个约定）。

### 追问面板：AI 起草等待态 + 「Close, skip summary」（`4881ae8`、`f0eae0c`）

- 新 `ChatWaiting` 组件：AI 把整段对话综合成结算草稿期间，面板内显示等待状态（取代旧的通用 `AiLoadingState`）。
- 线程列表的序号 chip 改成状态色：`open`=warning，`closed` 按 `finalVerdict` 分色（`user_correct`=success / `partial`=warning / `user_incorrect`=muted-foreground / 其余=primary），文字说明移进 tooltip。
- **新增「Close, skip summary」**：对 `dispute`/`score_challenge` 线程，除了原有「Summary」（AI 起草→确认/编辑/重新生成→提交）之外，现在还能直接关闭、什么都不生成——不跑 AI、不出改判结论、不写 `StandardOverride`、不碰 Band，效果等同一个纯知识追问的关闭。带独立的确认文案 `followUp.confirmCloseNoSummaryWarning`。后端新 `CloseFollowUpThreadCommand.SkipSummary` 标志承载（见后端「2026-09-09」一节，含两条新 `FollowUpThreadsControllerTests`）。
- 一处竞态修复：新线程发第一条消息后 `setActive(newId)` 并 invalidate 线程列表，那一次渲染 `threads.data` 还是创建前的旧列表、合法地不包含 `newId`——"线程不在列表里就退回 composer" 的守卫加了 `!threads.isFetching` 条件，否则会把刚创建的线程当成幽灵线程弹回去。
- `submission-page.tsx`：状态徽标从 `border-primary/30 text-primary` 改成 `border-transparent text-primary`（视觉重配色的连带调整）；批改失败等空态从「左对齐一段小字」改成「整卡居中」，呼应下面 93e7196 的 loading 态改版。

### 批改结果页显示官方通过线 + 批改/深入学习页 loading 态统一（`93e7196`）

- `grading-result-panel.tsx`：新 `parsePassBand(passThreshold)`（从 `assessment_dimensions.pass_threshold` 这类自由文本——"2" / "Band 2" / "Band 2 or above"——里取出第一个整数当通过线 Band）；额外拉一次 `listAssessmentDimensions(examType.id)` 按 `dimensionKey` 建 `passBandByKey` 映射，每条维度分数行的名称旁多显示一行「通过线 Band N」（`grading.passLineBand`）。每行的分隔线从实线改虚线、首末行不留多余内边距。
- `deep-learning-page.tsx`：Loading 态从通用 `AiLoadingState` 换成和批改页一致的「整卡居中大 spinner + 提示语」；页面 header 加返回按钮（`back` + `backHref` 指回来源批改页）；句型/词汇卡片改成虚线分隔、标签行搬到卡片顶部并按 domain/scenario/frequency 三类着色（频次高=primary、中=warning、其余=muted）。
- `app-shell.tsx`：`back`/`backHref` prop 放宽成显式 `| undefined`（配合上面深入学习页的可选返回入口）。

### `/exam-management` 六个管理面板设计翻新（`417d979`、`18cef05`）

覆盖考试类型列表页 + 六个配置 tab（评分维度、错误分类、题目分类、提示词模板、薄弱点目录）。**无接口变更**，纯组件重构 + i18n 新 key（`common.action`、`examMgmt.cat.domainTitle`/`scenarioTitle`、`examMgmt.wpc.mergeTitle` 等）：

- **考试类型列表**（`exam-management-page.tsx`）：去掉 Code 列；Subject 列去掉 `Badge` 改纯文字；Status 列改双色（enabled=`text-success`，disabled=`text-destructive`）；「Configuration」列改名「Action」，按钮从有边框文字按钮改成无边框图标按钮（`variant="ghost" size="icon"`），配 `Tooltip` 组件显示原文字标签。
- **评分维度 / 错误分类面板**：各去掉一列冗余的 `Key`/`code` 列（仍保留在新建表单里,因为后端要求）。
- **题目分类面板**（`categories-panel.tsx`）：去掉冗余的「System」列，改成按 `CategoryType`（domain/scenario）拆成两张表（各自标题「Domain categories」/「Scenario categories」），新建入口挂在第一张表；"Tag a question" 卡片里两个下拉框 + 按钮从「下拉两列网格 + 独立一行按钮」改成同一行（`flex sm:flex-row sm:items-center`，窄屏仍纵向堆叠）。
- **提示词模板面板**（`prompt-templates-panel.tsx`）：12 个用途分组的展示顺序从「按枚举值升序」改成产品指定的业务顺序（Question Generation → Grading → Follow-up → Follow-up Summary → Score Challenge Summary → Deep Learning → Vocabulary Semantic Drift → Weak Point Classification → Weak Point Detection Criteria → Weak Point Recheck → Progress Trend → Standard Revision，未来新增用途落到枚举值升序兜底）；启用/停用徽标从「实心背景」改成和薄弱点面板一致的「描边无底色」风格（`border-success/40 text-success` / `border-destructive/40 text-destructive`）。
- **薄弱点目录面板**（`weak-point-catalog-panel.tsx`）：去掉冗余的 `code` 列；状态徽标改用语义 token（active=success 描边、deprecated=destructive 描边、proposed 保留 warning 描边）；表格按 8 个一级种类拆成多张小表（未归类的行单独垫底一张，只在有数据时才渲染），并去掉每张表里重复的「Top-level category」列；**Merge（把 Source 折进 Target，Source 随之弃用）从常驻的一行控件收进一个 `Dialog` 弹窗**，触发按钮和第一张分类表的标题同一行显示。
- 「Exam Configuration」页头描述文案从直接罗列 6 个 tab 名字，改写成一句话概括（"Manage the grading rubric, AI prompts, and reference data that drive this exam type." / 中文对应句），不再和 tab 标签重复。
