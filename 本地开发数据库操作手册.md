# 本地开发数据库操作手册

后端可以连两个数据库之一：**线上 Supabase**（共享、真实数据）或**本地 Docker Postgres**（一次性、关掉即销毁）。
这份文档说明怎么切、怎么准备数据、怎么清理，以及为什么某些地方是这样设计的。

---

## 0. 一次性准备（每台机器只做一次）

密钥不在仓库里。Supabase 数据库的账号密码和大模型 API key 一样，放在 **.NET User Secrets**
（`%APPDATA%\Microsoft\UserSecrets\7ec4914d-9417-4f0d-bee9-04752fa77ee8\secrets.json`）：

```powershell
dotnet user-secrets set "Supabase:Username" "postgres.<project-ref>" --project src/DeepLearning.Api
dotnet user-secrets set "Supabase:Password" "<数据库密码>"            --project src/DeepLearning.Api
dotnet user-secrets set "Llm:Claude:ApiKey" "sk-ant-..."             --project src/DeepLearning.Api
# 其余 Llm:OpenAi:ApiKey / Llm:DeepSeek:ApiKey / Llm:Mimo:ApiKey 同理
```

`src/DeepLearning.Api/appsettings.Development.json` **是提交进 git 的**，里面只有非敏感部分：
主机、端口、库名、SSL 设置、公开的 Supabase 项目 URL、各家大模型的 BaseUrl 和型号。运行时由
[`ConnectionStringCredentials`](src/DeepLearning.Infrastructure/Persistence/ConnectionStringCredentials.cs)
把两半合起来。有一个测试会在这个文件里出现任何凭据时让构建失败，所以这条约束是被强制执行的，不是靠自觉。

前端另有 `src/DeepLearning.Web/.env.local`（见同目录 `.env.local.example`），它被 gitignore，
里面的 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 需要自己从 Supabase 控制台复制。

---

## 1. 日常操作表

### 连线上 Supabase（默认）

| 目的 | 命令 |
|---|---|
| 启动前后端 | `.\dev.ps1` |
| 确认当前连的是哪个库 | 浏览器打开 `http://localhost:5255/health/db` |

### 连本地临时库

| 目的 | 命令 |
|---|---|
| **新容器 + 拉线上数据 + 启动（推荐）** | `.\dev.ps1 -Db LocalDocker -DockerDbMode Ephemeral -PullReference` |
| 离线场景（不联网，用仓库种子脚本） | `.\dev.ps1 -Db LocalDocker -DockerDbMode Ephemeral -Bootstrap` |
| 容器已在跑，只想再起前后端 | `.\dev.ps1 -Db LocalDocker` |
| 需要跨重启保留数据 | 把 `-DockerDbMode Ephemeral` 换成 `Persistent` |

### 单独执行数据库命令

前提：容器已在跑。所有 `sql` / `db` 命令都不启动 Web 服务，跑完即退出。

| 目的 | 命令 |
|---|---|
| 从线上刷新参考数据（不重建容器） | `dotnet run --project src/DeepLearning.Api --launch-profile "http (LocalDocker)" -- db pull-reference` |
| 先看会拉哪些表，不动数据 | 上面这条末尾加 `--dry-run` |
| 空库离线建库 | `dotnet run --project src/DeepLearning.Api --launch-profile "http (LocalDocker)" -- sql bootstrap` |
| 看 SQL 脚本应用状态（线上） | `dotnet run --project src/DeepLearning.Api --launch-profile "http (Supabase)" -- sql status` |
| 把新增脚本应用到线上 | 同上，把 `sql status` 换成 `sql apply` |

### 跑测试

```powershell
dotnet test --filter "Category!=LlmIntegration"
```

测试用 Testcontainers 自己起容器、跑完自己销毁，**和上面的 dev 容器完全无关**，不需要任何清理，
也永远不会连线上库。

---

## 2. 清理测试数据

| 当前模式 | 销毁命令 | 说明 |
|---|---|---|
| `Ephemeral`（tmpfs） | `docker compose down` | 数据在内存里，`stop` / `restart` 也会丢，不占磁盘 |
| 默认（匿名卷） | `docker compose down -v` | **`-v` 不能省**，否则只删容器、留下悬空卷占磁盘 |
| `Persistent`（命名卷） | `docker compose -f docker-compose.yml -f docker-compose.persistent.yml down -v` | 不加 `-v` 数据会保留 |
| 忘了加 `-v` 的补救 | `docker volume prune` | 清掉悬空卷 |

只想清数据、不想重建容器：直接再跑一次 `db pull-reference` —— 它会先 `TRUNCATE ... CASCADE`
再重新灌，业务表里的测试数据会随 CASCADE 一起消失。

---

## 3. 三种数据准备方式的区别

| | `sql bootstrap` | `db pull-reference` |
|---|---|---|
| 数据来源 | 仓库里的 `Persistence/Sql/*.sql` 种子脚本 | 线上 Supabase 的真实行 |
| 是否需要联网 | 否 | 是 |
| 是否包含线上的手工改动 | **否** | 是 |
| `question_bank_categories` | 空（没有任何脚本插入过它） | 有 |
| `questions` 题库 | 空 | 有 |
| 适用场景 | 离线开发、CI、验证种子脚本本身 | 日常开发（默认选它） |

**为什么必须能「拉」而不是「重放脚本」**：线上库的 `assessment_dimensions`、`error_taxonomies`、
`prompt_templates` 都被手工改过，`question_bank_categories` 完全是通过 API 建出来的。
重放脚本得到的是「脚本描述的状态」——内部自洽，但和线上不一致，这是最坏的一种测试环境：
像到可以信任，错在关键处。

---

## 4. `db pull-reference` 会拉哪些表

**只拉白名单里的 15 张**，不是整库：

```
# 考试配置 / 参考数据
exam_types
assessment_dimensions
error_taxonomies
question_bank_categories
generation_policy
prompt_templates
llm_provider_settings
llm_provider_models

# 题库（questions 及其全部子表）
users                       ← 只因为 questions.created_by 指向它，见下
questions
meaning_checkpoints         ← 评卷时会读，缺了会按不同的要点集打分
task_b_seeded_errors        ← Task B 的预埋错误，缺了这道题就没得找
reference_translations      ← 参考译文
question_category_map       ← 题目 ↔ 分类映射，缺了没法按分类浏览题库
seed_reference_links        ← 生成题 ↔ 种子题的溯源
```

**刻意不拉**的是每个学习者自己产生的数据：`submissions`、`weak_points`、`grading_summaries`、
`follow_up_*`、`progress_*`、`ai_call_logs`。这些留在本地、随手销毁，才是临时库的意义。

一句话概括边界：**「题目本身是什么」全拉，「谁练了、练得怎么样」全不拉。**

白名单定义在
[`ReferenceDataSync.DefaultTables`](src/DeepLearning.Infrastructure/Persistence/ReferenceDataSync.cs)。
有测试保证：里面每张表都真实存在，且不含上面那批业务表。

### 关于 users 和 password_hash

后端**从不签发也从不校验密码**，登录 100% 由 Supabase Auth 负责，后端只验 JWT。所以：

- **不需要**给 `password_hash` 填任何值，填了也不会让本地库「可登录」——本地库根本不参与登录。
- 只要 `Supabase:ProjectUrl` 指向真实项目，前端照常登录；后端验完 JWT 后，
  `EnsureUserProfileMiddleware` 会自动在本地库补出这个用户的 `users` 行，`password_hash` 留空。
- `users` 之所以在白名单里，**只是因为 `questions.created_by` 是指向它的外键**。载入时外键触发器是
  关掉的（否则自引用的分类树会因行序报错），所以少拉 `users` 不会报错，而会留下一批指向不存在
  用户的 `created_by`——这种坏数据要很久以后才会暴露。所以只要拉 `questions`，就一定连 `users` 一起拉。

### 想加表

改 `DefaultTables` 即可。如果新表有外键父表，同时在同文件的 `RequiredCompanions` 里登记依赖——
依赖是**传递闭包**（`question_category_map` → `questions` → `users`），所以只登记直接父表就够了。

登记依赖不是可选的：载入时外键触发器是关掉的（否则自引用的分类树会因行序报错），少拉一张父表
**不会报错**，只会留下一批指向不存在记录的外键，很久以后才会以「作者是 null」「分类点不开」的形式暴露。

---

## 5. 怎么保证「用临时库时不会写线上库」

四道锁，都是结构性的（代码层面不可能绕过），不是靠注释约定：

1. **进程只有一个写入连接。** 后端全程只用 `ConnectionStrings:DefaultConnection` 一个连接串；
   LocalDocker profile 用环境变量把它覆盖成本地库。没有第二条写入路径。

2. **`DB_PROFILE` 与连接串强制交叉校验。**
   [`DatabaseTargetResolver`](src/DeepLearning.Infrastructure/Persistence/DatabaseTarget.cs)
   在启动时比对二者，不一致就**拒绝启动**：

   ```
   DB_PROFILE=LocalDocker but ConnectionStrings:DefaultConnection points at
   'aws-0-ap-southeast-2.pooler.supabase.com', which is not this machine. Refusing to start: ...
   ```

   反方向（声明 Supabase 却指向本机）同样拒绝——那意味着你以为在看共享库，其实在看一个空库。

3. **写入端必须在本机。** `sql bootstrap` 和 `db pull-reference` 都会先检查目标是否为本机地址，
   不是就直接退出，什么都不执行。`db pull-reference` 的写入端永远是 `DefaultConnection`（已被第 2 条
   校验过在本机），源库只出现在读取端——**没有任何参数组合能反过来往线上写**。

4. **源库连接是服务端级别只读的。** `db pull-reference` 打开线上连接后立刻执行
   `SET default_transaction_read_only = on`，任何写语句都会被 PostgreSQL 以 `25006` 拒绝。
   这样即使将来有人在这条代码路径上误加了写操作，也是在线上库直接报错，而不是悄悄写进去。

**随时确认当前连的是哪个库**：`GET http://localhost:5255/health/db`

```json
{"profile":"LocalDocker","declared":true,"isLocal":true,"host":"localhost","port":5433,"database":"deeplearning"}
```

`declared: false` 表示没有显式声明 `DB_PROFILE`（例如 `dotnet ef` 或测试宿主），是从主机名推断的。

> 历史教训：`dev.ps1` 曾把参数拼成 `--launchProfile`（正确写法是 `--launch-profile`）。
> dotnet run 不会对这个错误报错，而是把它当应用参数透传，然后**静默回退到 launchSettings 里的第一个
> profile，也就是 Supabase**。本来要连本地临时库的一次运行直接打在了线上库上，全程没有任何提示。
> 上面第 2 条就是为这件事加的。

---

## 6. 端口说明

Postgres 映射到宿主机 **5433**，不是 5432：本机已经跑着一个原生 Postgres 服务占了 5432。
Docker 的端口代理对已占用端口不会报错，`psql` / `dotnet ef` 会静默连到原生安装上，
表现为莫名其妙的认证失败。Redis 映射 6379，目前代码里还没用到，先留着。

---

## 7. 常见问题

**`refusing to apply: _sql_scripts has no baseline`**
`sql apply` 只能推进一个已有基线的库，不能用来建新库。空库请用 `sql bootstrap` 或 `db pull-reference`
（后者跑完会自动把全部脚本记为 baseline，之后 `sql apply` 就能正常用了）。

**`refusing to bootstrap: _sql_scripts already has N row(s)`**
这个库不是全新的。要么用 `db pull-reference` 刷新数据，要么 `docker compose down -v` 后重来。

**`-DockerDbMode` 好像没生效**
它只在容器**尚未运行**时生效。已经有容器在跑时 dev.ps1 会直接复用并提示。换模式要先 `down`。

**改了 `_manifest.txt` 后测试挂了**
`SqlScriptRunnerTests` 会断言脚本总数，新增脚本时同步改那个数字。

**新加的 .sql 脚本在全新库上跑不通**
EF 迁移已经把表建成最终形态，历史上的建表/改表脚本会和它冲突。这类脚本加进
`Persistence/Sql/_bootstrap_skip.txt`（附上原因），`sql bootstrap` 会记录但不执行它们。
