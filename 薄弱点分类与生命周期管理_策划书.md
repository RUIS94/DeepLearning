# 薄弱点分类与生命周期管理 —— 策划书 v3

> 状态:方案已确认,尚未编码。本文档是唯一的最终版本,后续如有调整请直接修订本文件。

## 0. 背景

现状问题:
1. `weak_point_catalog` 是扁平表,没有二级分类层级,依赖 `default_dimension_key`/`default_error_category` 做松散分组,无法区分"同一 (dimension, error_category) 组合下的不同错误模式"(例如 `numeric_statistical_traps`、`logical_relation_distortion`、`lookalike_word_confusion` 三者的分组字段完全相同)。
2. 薄弱点一旦被 AI 归类命中就立即 `active`,没有确认阈值。
3. 去激活完全依赖"5 次评判未复现"的陈旧度自动回收(`ResolveStaleWeakPointsAsync`),属于被动等待,系统不会主动验证"这个薄弱点是否真的已经解决"。
4. 没有可执行的"筛查标准"辅助后续判断,复核无据可依。

## 1. 两级分类体系

### 1.1 体系内容(用户提供,固定为闭集)

**一级分类(8 个,不再新增)**:

| code | 中文名 |
|---|---|
| semantic_errors | 语义错误 |
| lexical_errors | 词汇错误 |
| grammatical_syntactic_errors | 语法/句法错误 |
| information_errors | 信息错误 |
| discourse_logic_errors | 篇章/逻辑错误 |
| pragmatic_stylistic_errors | 语用/风格错误 |
| fluency_naturalness | 表达质量 |
| mechanics | 语言形式 |

**二级分类(叶子,挂在对应一级分类下)**:

- semantic_errors:Subject-Object Relationship(主客关系)、Negation(否定)、Modality(情态)、Causality(因果)、Temporal Relation(时间关系)、Spatial Relation(空间关系)、Comparison(比较)、Scope(范围)、Reference(指代)
- lexical_errors:Wrong Word Choice(词汇选择)、Terminology(术语)、Entity(实体)、Polysemy(多义词)、Collocation(搭配)
- grammatical_syntactic_errors:Tense/Aspect(时态/体)、Voice(语态)、Number(单复数)、Person(人称)、Word Order(语序)、Modifier/Attachment(修饰关系)
- information_errors:Omission(漏译)、Addition(增译)、Mistranslation(误译)、Distortion(语义偏移)
- discourse_logic_errors:Conjunction(连接关系)、Cohesion(衔接)、Coherence(连贯性)、Consistency(一致性)
- pragmatic_stylistic_errors:Register(语体)、Tone(语气)、Pragmatics(语用)、Cultural/Localization(文化/本地化)
- fluency_naturalness:Fluency(流畅性)、Naturalness(自然度)、Awkward Expression(生硬表达)
- mechanics:Punctuation(标点)、Formatting(格式)、Number/Date/Unit(数字/日期/单位)

共 8×N ≈ 38 条叶子,种子 SQL 阶段逐条敲定最终 code/name/description 文案。

### 1.2 落表方案

**`weak_point_categories`(新表,一级,全局共享)**
```
id, code, name, description, display_order
```

**`weak_point_catalog`(重构,二级/叶子,改为全局共享)**
```
id, category_id → weak_point_categories,
code(全局唯一,不再按 exam_type 区分), name, description,
status(proposed | active | deprecated), origin(seed | auto | manual), created_at
```
- 去掉原有 `exam_type_id` 列及 `(exam_type_id, code)` 唯一约束,`code` 改为全局唯一。
- 原因:这套分类是通用语言学范畴,与具体考试类型无关,全局共享一份即可,新增考试类型时无需重新定义。

### 1.3 旧数据迁移

现有 6 条 NAATI 专属 code(如 `numeric_statistical_traps`、`logical_relation_distortion`、`lookalike_word_confusion`、`passive_voice_unstable`、`terminology_level_confusion` 等)**逐条人工映射**到新体系里最贴切的叶子(例如 `numeric_statistical_traps` → `mechanics/number_date_unit`),历史 `weak_points.catalog_id` 批量改指向新叶子,旧 6 条 catalog 行迁移完成后下线。新体系完全取代旧体系,不并存。

### 1.4 归类时创建新叶子的路径

8 个一级分类是固定闭集,不新增;当 AI 在归类时判断现有全部叶子都不贴切,允许在归类结果里附带"建议新叶子"(名称 + 所属一级分类 + 描述)。后端据此创建一条 `status=proposed` 的新 `weak_point_catalog` 行,**该错误本次归类结果仍为 null,不使用尚未审核的新叶子**,待人工审核转 `active` 后才对后续错误生效。此机制与现有 `PromoteLegacyBucketAsync`(规则兜底桶自动转 proposed 供人工审核)保持一致的设计语言,不是另起一套。

## 2. 状态机

在 `WeakPointStatus` 基础上新增中间态:

```
[新错误首次归类到某叶子 code]
        │
        ▼
   tracking(观察中,occurrence_submission_count = 1)
        │  同一叶子 code 在新的提交中再次被命中(按提交去重计数)
        │  count += 1
        ▼
   count 达到 3?
   ├─ 否 → 保持 tracking
   └─ 是 → 转 active,加入本次待生成筛查标准的集合(见 §3 的 AI②)

   ── 之后 ──

   active 状态下,每次评判后走复核(AI③),得到:
   - resolved(有陷阱且处理对)   → 转 resolved,no_evidence_streak 清零
   - still_weak(有陷阱且处理错) → 保持 active,streak 清零
   - not_present(原文无该陷阱)  → 保持 active,streak += 1
                                    streak ≥ 5 → 转 resolved,streak 清零

   resolved 状态下,只要该 code 在任意新提交的归类结果中再次出现(不需要再攒 3 次):
   → 立即转 active(沿用既有 RecurrenceCount++/Priority=high 逻辑,不变)
   → 同时加入本次待生成筛查标准的集合,用新证据重新生成(不沿用旧的)

   无论当前处于 tracking / active / resolved 哪个状态,只要本次被命中,
   occurrence_submission_count 一律 += 1(终身累计,不清零);
   该字段仅在 status == tracking 时被读取用于判断是否达到 3 的阈值,
   命中后转入其他状态不再依赖它做任何判断,纯粹留存为历史计数。
```

**要点**:
- `tracking` 只用于"从未激活过"的薄弱点在满 3 次前的过渡期,不进入 grading 提示词(该查询条件仍是 `Status == active`)。
- 一旦真正激活过一次,后续"去激活 → 再复现"永远走既有的即刻复现通道,不再受阈值约束——复用现有机制,不重新设计这部分状态迁移。
- 原有"5 次评判未复现自动 resolved"的陈旧度回收逻辑(`ResolveStaleWeakPointsAsync`)整体移除,完全由 AI③ + `no_evidence_streak` 接管去激活判定权,避免两套逻辑互相打架。

## 3. 完整流程(含全部 AI 调用节点)

全部发生在评判完成之后的异步薄弱点任务里(与现有 `GenerateWeakPointsJob` 同一条异步链路),与评判本身的 Evidence / Proofread / Sweep / Verdict 多阶段流程无关,互不注入:grading 的 sweep 阶段仍然只读 `status==active` 的 `PatternSummary`/`Name`/`Recurring`,不读 `detection_criteria`,不感知 `tracking` 状态的薄弱点。

```
                        评判完成(error_list 已落库)
                                │
                                ▼
        ┌────────────────────────────────────────────────┐
        │ 【AI①】weak_point_classification(已有,需升级)    │
        │ 这一次调用内部捆绑两个子任务,与 AI②/AI③ 无关:     │
        │ ── 子任务 A(归类,与历史数据无关) ──                │
        │ 输入:本次 error_list 全部错误                     │
        │      + 全量二级叶子清单(按一级分类分组展示)        │
        │ 输出:每条错误 → 叶子 code(或 null,或"建议新叶子") │
        │ ── 子任务 B(顺带更新摘要,和归类是两回事) ──          │
        │ 输入:子任务 A 本次命中的 code + 该 code 现有的       │
        │      pattern_summary(旧摘要,用于合并,不用于归类)    │
        │ 输出:合并旧摘要与本次新证据后的 pattern_summary      │
        └────────────────────────────────────────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              │                                     │
    有"建议新叶子"的错误?                 按叶子 code 更新/创建 weak_points:
              │是                          - 命中(不论当前 tracking/active/resolved)
              ▼                              → occurrence_submission_count += 1(终身累计)
    创建 weak_point_catalog(proposed)      - status==tracking 且 count 从 <3 变为 ≥3
    该错误暂不归类,等待人工审核转正            → 转 active,加入"待生成筛查标准"集合
              │                            - status==active → 保持,streak 清零
              │                            - status==resolved → 立即转 active,
              │                              同样加入"待生成筛查标准"集合
              └─────────────────┬───────────────────┘
                                ▼
              本轮"待生成筛查标准"集合(首次达标的 + 复现重新生成的,合并去重)是否为空?
                    │是 ──────────────────────────────┐
                    │否                                │
                    ▼                                  │
        ┌──────────────────────────────────────┐       │
        │ 【AI②】weak_point_detection_criteria   │       │
        │ (集合内所有 code 一次调用,不分两条分支)  │       │
        │ 输入:每个 code 的定义                   │       │
        │      + 该用户此 code 下跨提交的           │       │
        │        【全部历史错误】(非本次单条示例)   │       │
        │ 输出:每个 code 各自的筛查标准规则文本     │       │
        │      (格式类似 grading 提示词里           │       │
        │       "【因与果不能互换】…"的规则片段)     │       │
        └──────────────────────────────────────┘       │
                    │                                  │
                    ▼                                  │
          写各自 detection_criteria(新激活的同时         │
          完成 tracking → active 状态落地)               │
                    │                                  │
                    └────────────────┬──────────────────┘
                                     ▼
        取该用户 status=active 且【本次未被①命中】的全部 code
                    │
              列表为空? ─是→ 结束,跳过复核
                    │否
                    ▼
        ┌──────────────────────────────────────┐
        │ 【AI③】weak_point_recheck(新增)        │
        │ 输入:待复核 code 列表(各自 detection_   │
        │      criteria)+ 本次原文全文 + 用户译文全文 │
        │ 输出:逐 code 三态之一:                  │
        │      resolved / still_weak / not_present │
        └──────────────────────────────────────┘
                    │
        ┌───────────┼────────────────┐
        ▼           ▼                ▼
    resolved     still_weak      not_present
    → resolved   保持 active,     保持 active,streak += 1
    streak 清零   streak 清零     streak ≥ 5 → resolved,streak 清零
```

**成本**:阶段二(AI②)几乎总是 0 次调用,只在阈值刚跨越或复现时触发;阶段三(AI③)在用户有 active 薄弱点时恒定 1 次调用,不随 active 数量线性增长(合并成一个 list 一次处理)。

**不产生"假犯错记录"**:AI②、AI③ 全程不写 `error_list`,不新增 `weak_point_occurrences`,不触发 `RecurrenceCount`——`RecurrenceCount` 只在"resolved → 新提交命中而复现"这一条既有路径上按原逻辑触发。

## 4. 表结构变更汇总

| 表 | 变更 |
|---|---|
| `weak_point_categories`(新表) | 一级分类,8 条固定种子数据,全局共享 |
| `weak_point_catalog` | 新增 `category_id` FK;去掉 `exam_type_id` 及其唯一约束,`code` 改全局唯一;旧 6 条 NAATI code 数据迁移后下线 |
| `weak_points.status` | 枚举新增 `tracking` |
| `weak_points` | 新增 `occurrence_submission_count int default 0`(每次命中终身累计 +1,不清零;仅在 `status==tracking` 时被读取用于判断是否达到 3 的阈值) |
| `weak_points` | 新增 `detection_criteria text, nullable`(AI② 产出,AI③ 复核时作为该 code 的判断依据注入) |
| `weak_points` | 新增 `no_evidence_streak int default 0`(AI③ 连续 `not_present` 计数,达 5 转 resolved;命中本次归类或 `still_weak` 时清零) |
| 移除逻辑 | `ResolveStaleWeakPointsAsync`(陈旧度自动回收)整体删除 |

## 5. 新增 AI 调用类型(`AiOperationType`)

- `weak_point_classification`(已有,prompt 需升级为支持新的两级叶子清单 + "建议新叶子"输出)
- `weak_point_detection_criteria`(新增,AI②,首次达标/复现两种触发场景共用同一 prompt)
- `weak_point_recheck`(新增,AI③)

## 6. 留待编码阶段确认的细节

1. `Submissions` 表是否已保存原文/译文全文(而非仅 snippet),`weak_point_recheck` 需要完整文本,实现前需核实字段是否存在,不存在则需先补充。
2. 38 条具体叶子的最终英文/中文命名、description 文案,种子 SQL 阶段逐条敲定。
3. `weak_points` 的唯一约束目前是 `(user_id, catalog_id)`,不区分 exam_type;新分类体系改为全局共享后,同一用户跨考试类型的同一叶子薄弱点是否应合并为一条记录,需在实现阶段确认是否符合预期(初步判断:符合,因为该分类体系本身就是跨考试类型通用的语言学范畴)。
