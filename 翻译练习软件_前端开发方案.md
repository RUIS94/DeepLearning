# 翻译练习软件 —— Next.js 前端开发方案

> 本文档是给 AI 辅助编码使用的**指导性文件**，不是需求文档本身（需求见 [翻译练习软件_设计文档.md](翻译练习软件_设计文档.md)）。目标是把"后端已经提供了什么、前端要怎么架、每个页面做什么、用哪些接口、组件怎么复用"这几件事一次性钉死，减少后续 AI 辅助开发时来回猜测/返工。
>
> 编写时间：2026-08-31。后端状态：设计文档 Step 0-9 全部完成并有 258 个自动化测试覆盖，Step 10（生产加固）未开始；本文档中列出的所有接口/DTO 字段均直接从当前后端源码核对，不是从设计文档推断的。若后端后续变更，本文档需要同步更新（尤其是"契约细节"一节）。

---

## 目录

1. [技术选型与架构决策](#1-技术选型与架构决策)
2. [项目结构](#2-项目结构)
3. [与后端桥接的关键契约细节（必读）](#3-与后端桥接的关键契约细节必读)
4. [鉴权与会话方案](#4-鉴权与会话方案)
5. [API 客户端与后端代理层](#5-api-客户端与后端代理层)
6. [类型层：TypeScript 类型定义](#6-类型层typescript-类型定义)
7. [路由与页面清单](#7-路由与页面清单app-router)
8. [组件库与复用架构](#8-组件库与复用架构)
9. [关键业务流程的前端实现细节](#9-关键业务流程的前端实现细节)
10. [状态管理与数据获取策略](#10-状态管理与数据获取策略)
11. [表单与校验](#11-表单与校验)
12. [样式与设计系统](#12-样式与设计系统)
13. [测试策略](#13-测试策略)
14. [环境变量与部署](#14-环境变量与部署)
15. [分阶段开发路线图](#15-分阶段开发路线图)
16. [已知坑与对后端的建议](#16-已知坑与对后端的建议)

---

## 1. 技术选型与架构决策

| 领域 | 选型 | 理由 |
|---|---|---|
| 框架 | **Next.js 15+，App Router**（不用 Pages Router） | 见下方专门说明 |
| 语言 | TypeScript，`strict: true` | 后端是强类型 C#，前端不开 strict 等于把契约优势丢一半 |
| 样式 | Tailwind CSS + **shadcn/ui**（Radix primitives，代码直接拷进项目而非黑盒依赖包） | shadcn 组件本质是"抄进你项目里的源码"，天然就是可复用、可改的基础组件层，正好满足"组件复用"的要求；比引入 Ant Design 这种整体重 UI 库更灵活，也比纯手写 Radix 省时间 |
| 图标 | lucide-react | shadcn 默认搭配 |
| 数据请求/缓存（客户端） | **TanStack Query v5** | 所有"提交、评分、追问、标记复习"等有副作用的调用都需要 loading/错误态/失效重取，React Query 是这类场景的标准解法，避免每个组件手写 `useState<'idle'\|'loading'\|'error'>` |
| 数据请求（服务端） | Next.js Server Components 直接 `fetch` 后端 | 只读数据（题目详情、进度、题库列表等）尽量在服务端拿，减少客户端 JS 和请求瀑布 |
| 表单 | React Hook Form + Zod | 见第 11 节 |
| 鉴权 | `@supabase/ssr` + `@supabase/supabase-js` | 后端已经把注册/登录完全交给 Supabase Auth（见第 4 节），前端职责就是"用 Supabase 官方 SDK 处理会话，把 token 转发给后端" |
| 图表 | Recharts | 设计文档 §7 明确点名"Recharts 绘制学习曲线/进度仪表盘"，没有理由换 |
| 富文本/标注 | 不引入富文本编辑器，TaskB 标注用**自定义的字符偏移选区组件**（见第 9.2 节） | 后端 TaskB 提交的数据结构是"字符偏移量数组"，不是富文本 delta，引入 Slate/TipTap 之类反而要多做一层转换，纯自定义组件更贴合数据形状 |
| 测试 | Vitest + React Testing Library（单元/组件），Playwright（E2E） | 与后端"单元/集成/API"三层测试金字塔的思路对应，设计文档 §11.2 Step 10 也点名了 Playwright |

### 为什么是 App Router，不是 Pages Router

直接给结论：**用 App Router**。理由：

1. **练习流程天然是嵌套路由**：`/practice/[questionId]/answer` → 提交后进入 `/practice/[questionId]/result` → 追问弹窗/深入学习都挂在同一个题目上下文里。App Router 的 `layout.tsx` 嵌套 + 平行路由（parallel routes）正好表达这种"外层题目上下文不变、内层内容切换"的结构，Pages Router 要嵌套布局得手写 `_app.tsx` 拼层级，很快会乱。
2. **AI 调用是同步阻塞的慢请求**：评分（`POST /submissions/{id}/grade`）、出题、追问、深入学习都是等 LLM 回复才返回，重试执行器最坏情况下会有 2s/4s/8s 退避（见第 3.4 节），也就是单次请求可能长达十几秒。App Router 每个路由段自带 `loading.tsx`/`error.tsx` 边界，配合 Suspense 天然适合表达"这个区域在转圈，其余页面正常"，不用每个页面自己拼 loading 状态机。
3. **只读页面用 Server Component 直连后端**：题库列表、进度看板、复习库这些纯展示页面完全不需要客户端 JS 去发请求——Server Component 里 `fetch` 一次，服务端渲染好直接吐给浏览器，减少一趟客户端请求和一份 loading 骨架屏代码。Pages Router 的 `getServerSideProps` 能做到类似效果，但组件层面拿不到"部分页面用 RSC、部分用客户端组件"这种混合粒度。
4. **Route Handler 天然是后端代理层的落点**：见第 5 节，`app/api/backend/[...path]/route.ts` 这种 catch-all 代理必须是 App Router 的 Route Handler，Pages Router 的 API Routes 做不了同样干净的 catch-all + 流式转发。
5. Next.js 官方投入重心早已在 App Router，Pages Router 是维护模式——新项目没有理由选旧范式。

代价（如实说明，不回避）：App Router 的心智模型（Server/Client Component 边界、`"use client"` 何时加）比 Pages Router 陡一些，AI 辅助编码时偶尔会分不清一个组件该是 Server 还是 Client——本文档第 10 节给了明确的判断规则，照着套即可。

---

## 2. 项目结构

```
frontend/                              # 独立于 .NET 解决方案的 Next.js 项目根目录
├── app/
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── layout.tsx                 # 未登录态的极简布局
│   ├── (main)/                        # 登录后的主布局（导航栏、用户菜单）
│   │   ├── layout.tsx
│   │   ├── practice/
│   │   │   ├── page.tsx               # 题库浏览 + 出题入口
│   │   │   ├── [questionId]/
│   │   │   │   ├── page.tsx           # 答题页（TaskA/TaskB）
│   │   │   │   ├── layout.tsx         # 该题目的上下文 Provider（题目详情预取）
│   │   │   │   └── loading.tsx
│   │   │   └── generate/page.tsx      # AI 出题配置页
│   │   ├── submissions/
│   │   │   └── [submissionId]/
│   │   │       ├── page.tsx           # 评分结果页（含追问入口）
│   │   │       └── loading.tsx
│   │   ├── deep-learning/
│   │   │   └── [questionId]/page.tsx  # 深入学习（参考译文/句型/词汇）
│   │   ├── review-library/
│   │   │   └── page.tsx               # 跨题目复习库浏览
│   │   ├── weak-points/
│   │   │   └── page.tsx               # 薄弱点列表
│   │   ├── progress/
│   │   │   └── page.tsx               # 学习曲线仪表盘
│   │   └── standard-overrides/
│   │       └── page.tsx               # 追问引发的评分标准修正记录（只读追溯页）
│   ├── admin/                         # 内容管理后台（无角色系统，见第 16 节）
│   │   ├── layout.tsx
│   │   ├── exam-types/page.tsx
│   │   ├── exam-types/[examTypeId]/dimensions/page.tsx
│   │   ├── exam-types/[examTypeId]/error-taxonomies/page.tsx
│   │   ├── prompt-templates/page.tsx
│   │   ├── llm-providers/page.tsx
│   │   ├── question-bank-categories/page.tsx
│   │   └── questions/import/page.tsx  # 手工导入题目/真题种子
│   ├── api/
│   │   └── backend/
│   │       └── [...path]/route.ts     # 通用后端代理（第 5 节）
│   ├── layout.tsx                     # 根布局：字体、全局 Provider
│   └── globals.css
├── components/
│   ├── ui/                            # shadcn/ui 生成的基础组件（button, dialog, table, ...）
│   ├── practice/                      # 练习流程专用组件
│   │   ├── task-a-editor.tsx
│   │   ├── task-b-annotator.tsx
│   │   ├── selectable-source-text.tsx # 字符偏移选区核心组件
│   │   ├── question-card.tsx
│   │   └── difficulty-badge.tsx
│   ├── grading/
│   │   ├── grading-result-panel.tsx
│   │   ├── dimension-band-row.tsx
│   │   ├── error-list-item.tsx
│   │   └── follow-up-dialog.tsx
│   ├── deep-learning/
│   │   ├── reference-translation-card.tsx
│   │   ├── sentence-pattern-card.tsx
│   │   └── vocab-expression-card.tsx
│   ├── progress/
│   │   ├── band-trend-chart.tsx
│   │   └── pass-rate-chart.tsx
│   ├── weak-points/
│   │   └── weak-point-card.tsx
│   ├── admin/
│   │   └── crud-table.tsx             # 通用后台增改列表（复用于 exam-types/categories/...）
│   └── shared/
│       ├── ai-loading-state.tsx       # 统一的"AI 正在处理"占位（见 9.3 节）
│       ├── error-banner.tsx           # 统一的 ProblemDetails 渲染组件
│       └── enum-select.tsx            # 通用枚举下拉（配合 6.2 节的枚举元数据）
├── lib/
│   ├── api/
│   │   ├── fetcher.ts                 # 统一 fetch 封装（第 5 节）
│   │   ├── exam-types.ts              # 每个资源一个文件：函数 + React Query hooks
│   │   ├── questions.ts
│   │   ├── submissions.ts
│   │   ├── follow-ups.ts
│   │   ├── standard-overrides.ts
│   │   ├── weak-points.ts
│   │   ├── progress.ts
│   │   ├── review-library.ts
│   │   ├── question-bank.ts
│   │   └── llm-providers.ts
│   ├── types/
│   │   ├── enums.ts                   # 第 6.2 节：枚举 + 序数映射表
│   │   └── dtos.ts                    # 第 6.3 节：所有请求/响应类型
│   ├── auth/
│   │   ├── supabase-server.ts         # 服务端 Supabase client（读 cookie）
│   │   ├── supabase-client.ts         # 浏览器端 Supabase client
│   │   └── session.ts                 # 取当前用户/token 的统一入口
│   ├── query-client.ts                # React Query 的 QueryClient 单例 + Provider
│   └── utils.ts                       # cn()、日期/数字格式化等
├── hooks/
│   ├── use-exam-config.ts             # 全局唯一 ExamType 的缓存（第 9.1 节）
│   └── use-current-user.ts
├── middleware.ts                      # Supabase 会话刷新
├── next.config.ts
├── .env.local.example
└── package.json
```

**复用设计原则**（贯穿整个结构）：

- **一个后端资源 = 一个 `lib/api/*.ts` 文件**，里面既有纯函数（`getQuestionById`、`createSubmission`）也有对应的 React Query hooks（`useQuestion`、`useCreateSubmissionMutation`）——组件永远不直接 `fetch`，永远走这一层。
- **业务组件按"领域"分文件夹，不按"页面"分**（`components/practice/`、`components/grading/` 而不是 `components/practice-page/`），因为同一个 `GradingResultPanel` 会同时出现在"提交后立即展示"和"历史提交详情页"两个页面里。
- **admin 的 6 个 CRUD 页面共用一个 `crud-table.tsx`**（列定义 + 表单 schema 作为 props 传入），而不是每个资源写一遍表格——这 6 个资源（exam-types/dimensions/error-taxonomies/prompt-templates/question-bank-categories 等）的操作模式高度一致（List + Create，无 Update/Delete，见第 3.5 节）。

---

## 3. 与后端桥接的关键契约细节（必读）

这一节是全文档最容易被忽略、也最容易在开发中间踩坑的部分。**每一条都已经对照后端源码核实**，不是猜测。

### 3.1 基础信息

- API 前缀：`/api/v1`（`ApiRoutes.Prefix`）。
- 后端地址：本地开发默认 `https://localhost:<port>`（看 `launchSettings.json`），生产环境是一个独立部署的 .NET 服务，**不会**和 Next.js 部署在同一个域名/端口下。
- 后端**没有配置 CORS**（`Program.cs` 里没有 `AddCors`/`UseCors`）。这不是疏漏要去修（虽然第 16 节给了一个可选的后端建议），而是本方案的架构前提：**浏览器永远不直接请求 .NET 后端**，全部经过 Next.js 自己的 Route Handler 代理（第 5 节），代理是服务器到服务器的调用，天然没有 CORS 限制。

### 3.2 错误响应格式（RFC 7807 ProblemDetails）

所有非 2xx 响应都是这个形状（`GlobalExceptionHandler.cs`）：

```json
{
  "status": 404,
  "title": "Question with id xxxx was not found.",
  "correlationId": "3f2c1a...",
  "errors": {
    "DimensionKey": ["'Dimension Key' must not be empty."]
  }
}
```

- `errors` 字段**只在 400（FluentValidation 校验失败）时出现**，是"字段名 → 错误信息数组"的字典。
- `correlationId` 对应响应头 `X-Correlation-Id`（每个请求后端都会回传，无论成功失败）——前端应该在全局错误提示里带上这个值（哪怕只是"错误码：xxx，方便反馈问题"这种小字），方便对照后端日志排查。
- 状态码含义：`400` 校验失败或状态机非法转换、`404` 资源不存在、`409` 冲突（如状态机非法跳转、重复标签）、`503` **AI 调用失败**（重试耗尽后）、`500` 未预期异常。前端的统一错误处理组件（`error-banner.tsx`）要能区分这几种，尤其 `503` 应该展示"AI 暂时不可用，请重试"而不是笼统的"出错了"。

### 3.3 枚举在 JSON 里是数字，不是字符串 ⚠️

**这是最容易踩的坑**：后端没有全局注册 `JsonStringEnumConverter`，所以 `System.Text.Json` 用默认行为——**所有枚举字段序列化成整数序数**，不是像 `"medium"` 这样的字符串。例如 `GetQuestionByIdResult.Difficulty` 返回的是 `1`（对应 `medium`），不是 `"medium"`。

反过来，**发请求时枚举字段也要传数字**（如 `POST /questions/generate` 的 `TaskType` 字段传 `0` 表示 TaskType.A）。

第 6.2 节给出了每个用到的枚举的完整"序数 → 标签"映射表，前端类型层必须严格按这张表写，不能凭直觉猜（比如 `Difficulty` 是 `easy=0/medium=1/hard=2`，但 `Priority` 是 `high=0/medium=1/low=2`——两个"1"含义完全不同，肉眼看数字猜不出来）。

第 16 节给了一个可选的后端小改动建议（加一行 `JsonStringEnumConverter` 全局配置），如果后端愿意改，这条坑就不存在了，但**在后端明确这么改之前，前端必须按"数字契约"实现**。

### 3.4 AI 相关接口可能很慢，要有对应的等待态设计

以下接口内部会调用 LLM，且失败后端会自动重试（design doc §4.2 的重试子状态机，2s/4s/8s 指数退避，最多 3 次）：

| 接口 | 说明 |
|---|---|
| `POST /questions/generate` | AI 出题 |
| `POST /submissions/{id}/grade` | AI 评分 |
| `POST /follow-ups` | AI 回答追问 |
| `POST /questions/{id}/deep-learning` | AI 生成深入学习内容（**首次生成慢，命中缓存后瞬间返回**，见 3.6 节） |

单次请求最坏情况下（3 次重试全部经历完整退避）可能长达 **十几秒**。前端必须：

1. 给这几个按钮加防抖/禁用态（点击后立即禁用，避免用户以为没反应而连点）。
2. 用统一的 `ai-loading-state.tsx` 组件展示"AI 正在批改/出题/回答中，可能需要几秒到十几秒"这种明确预期，而不是一个模糊的转圈。
3. 客户端 fetch 的 timeout 设置要比后端最坏情况更宽松（建议前端侧 timeout ≥ 30s），否则会出现"后端还在重试，前端先超时报错"的假失败。

### 3.5 大部分列表接口没有分页

`Application/Common/{PagedRequest,PagedResult}` 目前是空壳，从未被任何 Handler 使用——所有 `List*` 接口（题目列表、复习库、追问标准修正记录等）都是**一次性返回全部结果的数组**，没有 `page`/`pageSize` 参数。短期数据量小没问题，但前端做列表页时：

- 不要设计分页控件去调用不存在的分页参数。
- 如果某个列表未来数据量可能变大（比如题库、复习库），考虑前端自己做客户端分页/虚拟滚动，而不是指望后端接口支持。
- 这属于后端已知的、故意延后的设计（design doc §9.7 的 YAGNI 原则），不是前端能单方面解决的，需要就位再联动后端加分页。

### 3.6 "生成"类接口大多是幂等的，前端不需要自己做防重复调用的复杂逻辑

- `POST /questions/{id}/deep-learning`：**按 Question 幂等**，不是按用户/提交幂等。第一次调用触发 AI 生成并落库，之后任何用户对同一题目再调用都直接返回缓存内容（响应里有 `wasCached: boolean` 字段，可以用来决定是否要提示"这是之前生成的内容"）。
- `POST /questions/generate`：**不幂等**，每次调用都会真的生成一道新题（如果想要"手动指定真题种子"或"命中薄弱点"等定向出题，用请求体里的 `seedQuestionIds`/`targetWeakPoints` 字段，见第 6.3 节）。

### 3.7 CRUD 接口的读写不对称（Create+GetById+List，没有 Update/Delete）

以下资源目前**只有创建和查询接口，没有修改/删除**（后端自己的说法是"reference data, append-only"约定）：exam-types、assessment-dimensions、error-taxonomies、prompt-templates、question-bank-categories。管理后台的这几个页面**不要设计"编辑"或"删除"按钮**，只做"新建 + 列表浏览"。`assessment-dimensions` 比较特殊："新建一条新版本"的语义是"自动关闭上一条版本的有效期"（见第 3.8 节），前端应该把它包装成"修订规则版本"而不是普通的新建。

### 3.8 评分维度（assessment-dimensions）有版本化语义

`POST /exam-types/{examTypeId}/assessment-dimensions` 创建的是"某个 `dimensionKey` 的新版本"：

- 如果同一 `dimensionKey` 已存在一条 `effectiveTo` 为空（仍生效）的记录，新记录的 `effectiveFrom` 必须晚于旧记录的 `effectiveFrom`，否则后端返回 400。
- 创建成功后，旧版本的 `effectiveTo` 会被自动设置为新版本的 `effectiveFrom`（后端原子操作，前端不用管，只需要知道"新建"这个动作在语义上更接近"修订生效日期"）。
- `GET /exam-types/{examTypeId}/assessment-dimensions` 默认**只返回当前生效窗口内**的版本（`effectiveFrom <= now < effectiveTo`），不会返回历史版本——如果后台管理页要展示历史版本审计，需要另外设计（当前后端没有专门的"查历史版本"接口，只能拿到当前生效的）。

### 3.9 提交内容（`Content` 字段）是"字符串里套 JSON"，不是普通对象

`POST /submissions` 的 `content` 字段本身是一个**字符串**，但这个字符串的内容必须是合法 JSON，且根据 `taskType` 有不同形状：

```ts
// TaskType.A（普通翻译）：Content 是 JSON 编码的字符串
{ questionId, userId, taskType: 0, content: JSON.stringify("我的译文全文...") }

// TaskType.B（找错标注）：Content 是 JSON 编码的对象数组
{
  questionId, userId, taskType: 1,
  content: JSON.stringify([
    { positionStart: 12, positionEnd: 20, errorCategory: "distortion", correctedText: "修正后的文本" }
  ])
}
```

`positionStart`/`positionEnd` 是**字符偏移量，相对于该题目的 `flawedTranslationText` 字段**（不是相对于用户输入框），必须和后端种子错误（`taskBSeededErrors`）用同一套坐标系——这也是为什么第 9.2 节要单独设计一个"字符偏移选区组件"而不是随便拿个富文本编辑器。

### 3.10 用户身份是"可选携带，JWT 优先"，不是强制鉴权

后端**没有任何接口标 `[Authorize]`**（design doc 之外的用户决策）。每个需要用户身份的接口都是"如果请求带了合法 JWT，用 JWT 里的 `sub`；没带就退回请求体/查询参数里显式传的 `userId`"。这意味着：

- 理论上不登录也能调用大部分接口（只要肯在请求体里塞一个 `userId`）——但前端产品设计上应该当作**必须登录**来做（未登录不允许进入练习流程），只是技术上后端不会因为没 token 就拒绝。
- 一旦用户登录，前端**必须**把 JWT 放进请求（走第 5 节的代理层自动注入），而不是继续手动传 `userId` 字段——传两个不一致的身份，后端会以 JWT 为准，容易让前端自己的状态和后端实际写入的数据对不上。

---

## 4. 鉴权与会话方案

后端的鉴权模型（AGENTS.md "Auth (Supabase JWT)" 一节）：**注册/登录 100% 是 Supabase Auth 自己的事**，.NET 后端只验证一个已经签发好的 JWT（`Authority` 指向 `{ProjectUrl}/auth/v1`，非对称 JWKS 签名）。这对前端意味着：

1. **不要**自己写登录/注册表单去调用 .NET 后端——那里根本没有这个接口（`UsersController` 只有 `GetById`）。
2. 用 `@supabase/supabase-js` + `@supabase/ssr` 官方套路：
   - `lib/auth/supabase-client.ts`：`createBrowserClient(url, anonKey)`，给客户端组件里的登录表单、`supabase.auth.signInWithPassword(...)` 等用。
   - `lib/auth/supabase-server.ts`：`createServerClient(url, anonKey, { cookies })`，给 Server Component / Route Handler 读当前会话用。
   - `middleware.ts`：官方推荐的 `updateSession` 模式，每个请求刷新一次 Supabase 的 cookie（避免 access token 过期后用户莫名其妙被登出）。
3. **首次访问时，`EnsureUserProfileMiddleware`（后端）会自动在 `public.users` 建一条 profile 行**——前端不需要在注册后额外调用某个"创建用户资料"接口，第一次带 token 打后端任意接口就会自动建好。
4. 拿到 Supabase session 后，`session.access_token` 就是要转发给 .NET 后端的 JWT——第 5 节的代理层会自动从当前请求的 Supabase cookie 里取这个 token 加到 `Authorization: Bearer <token>` 头上，业务代码不需要手动传。
5. 页面级鉴权：`(main)` 路由组的 `layout.tsx` 里检查 `session`，没有就 `redirect('/login')`；`admin` 路由组同理（但见第 16 节——admin 目前只是"前端约定俗成"的保护，后端并不区分角色）。

---

## 5. API 客户端与后端代理层

### 5.1 为什么要一个代理层

第 3.1 节说过：后端没配 CORS，浏览器不能跨源直连。方案：

- **Server Component 里的只读请求**：服务器到服务器，直接 `fetch(process.env.BACKEND_API_BASE_URL + '/api/v1/...')`，不经过代理（没有 CORS 问题，因为发起方不是浏览器）。
- **Client Component 里的请求（表单提交、按钮点击触发的 mutation）**：必须经过 Next.js 自己的同源 Route Handler 代理到后端，浏览器只跟 Next.js 自己的域打交道。

### 5.2 通用代理 Route Handler

```ts
// app/api/backend/[...path]/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { getAccessToken } from '@/lib/auth/session';

const BACKEND_BASE_URL = process.env.BACKEND_API_BASE_URL!; // 服务端专用，不加 NEXT_PUBLIC_ 前缀

async function proxy(req: NextRequest, path: string[]) {
  const token = await getAccessToken(); // 从 Supabase server client 读当前会话
  const targetUrl = `${BACKEND_BASE_URL}/api/v1/${path.join('/')}${req.nextUrl.search}`;

  const headers = new Headers();
  headers.set('Content-Type', 'application/json');
  if (token) headers.set('Authorization', `Bearer ${token}`);
  // 透传 correlation id，方便前后端日志对上（见 3.2 节）
  const incomingCorrelationId = req.headers.get('x-correlation-id');
  if (incomingCorrelationId) headers.set('X-Correlation-Id', incomingCorrelationId);

  const body = ['GET', 'HEAD'].includes(req.method) ? undefined : await req.text();

  const upstream = await fetch(targetUrl, { method: req.method, headers, body });

  const responseHeaders = new Headers();
  const correlationId = upstream.headers.get('x-correlation-id');
  if (correlationId) responseHeaders.set('X-Correlation-Id', correlationId);

  return new NextResponse(upstream.body, { status: upstream.status, headers: responseHeaders });
}

export async function GET(req: NextRequest, { params }: { params: Promise<{ path: string[] }> }) {
  return proxy(req, (await params).path);
}
export const POST = GET, PATCH = GET, DELETE = GET; // 同一份转发逻辑，方法名从 req.method 读
```

（真实实现要给每个动词单独导出函数，上面这行伪代码只是表达"逻辑完全复用"，AI 编码时按 Next.js 的 Route Handler 规范展开成 `export async function POST(...)` 等。）

### 5.3 统一 fetcher（`lib/api/fetcher.ts`）

```ts
type FetchOptions = { method?: string; body?: unknown; query?: Record<string, string | number | boolean | undefined> };

// 服务端组件用：直连后端。客户端组件用：走 /api/backend 代理。
// 用同一个函数签名，只是 baseUrl 不同，避免业务代码分裂成两套写法。
export function createApiClient(baseUrl: string, getAuthHeader?: () => Promise<string | null>) {
  return async function apiFetch<T>(path: string, options: FetchOptions = {}): Promise<T> {
    const url = new URL(`${baseUrl}${path}`);
    for (const [k, v] of Object.entries(options.query ?? {})) {
      if (v !== undefined) url.searchParams.set(k, String(v));
    }

    const headers: Record<string, string> = { 'Content-Type': 'application/json' };
    const authHeader = await getAuthHeader?.();
    if (authHeader) headers.Authorization = authHeader;

    const res = await fetch(url, {
      method: options.method ?? 'GET',
      headers,
      body: options.body ? JSON.stringify(options.body) : undefined,
    });

    if (!res.ok) {
      const problem = await res.json().catch(() => null);
      throw new ApiError(res.status, problem);
    }
    if (res.status === 204) return undefined as T;
    return res.json();
  };
}

export class ApiError extends Error {
  constructor(public status: number, public problem: ProblemDetails | null) {
    super(problem?.title ?? `Request failed with status ${status}`);
  }
}
```

- Server Component：`const api = createApiClient(process.env.BACKEND_API_BASE_URL!, getServerAccessTokenHeader)`。
- Client Component（经代理）：`const api = createApiClient('/api/backend', undefined)`（代理层自己注入 token，客户端不需要重复处理）。

### 5.4 每个资源一个 API 模块 + React Query hooks

```ts
// lib/api/submissions.ts
export function createSubmission(api: ApiClient, req: CreateSubmissionRequest) {
  return api<CreateSubmissionResult>('/submissions', { method: 'POST', body: req });
}
export function gradeSubmission(api: ApiClient, id: string, examTypeId: string) {
  return api<GradeSubmissionResult>(`/submissions/${id}/grade`, { method: 'POST', body: { examTypeId } });
}
export function getSubmissionById(api: ApiClient, id: string) {
  return api<GetSubmissionByIdResult>(`/submissions/${id}`);
}

// 客户端 hooks（用第 5.3 节的代理版 api client）
export function useCreateSubmission() {
  const api = useApiClient();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (req: CreateSubmissionRequest) => createSubmission(api, req),
  });
}
export function useGradeSubmission(submissionId: string) {
  const api = useApiClient();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (examTypeId: string) => gradeSubmission(api, submissionId, examTypeId),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['submission', submissionId] }),
  });
}
```

这一层是"逻辑复用"的核心落点：任何组件要交题、要评分、要看历史提交，都从这几个 hook 里拿，不允许在组件里直接拼 URL。

---

## 6. 类型层：TypeScript 类型定义

### 6.1 组织方式

`lib/types/enums.ts` 放枚举 + 序数映射，`lib/types/dtos.ts` 放请求/响应类型，一一对应后端的 C# record。**不要**尝试用 `openapi-typescript` 之类工具从后端的 `/openapi/v1.json` 自动生成后完全替代手写——后端的 OpenAPI 描述对"哪个字段可空""枚举当整数还是字符串"这类细节的表达未必精确，手写这一层反而是更可靠的契约来源；但建议**定期跑一次 `openapi-typescript` 生成对照，用来发现手写类型是否和后端契约"跑偏"**（后端字段改了名字/加了字段而前端没同步，diff 出来一眼看到）。

### 6.2 枚举与序数映射表（⚠️ 已核对后端源码，逐条可信）

```ts
// lib/types/enums.ts

export const TaskType = { A: 0, B: 1 } as const;
export const Difficulty = { easy: 0, medium: 1, hard: 2 } as const;
export const QuestionOrigin = { ai_generated: 0, user_uploaded: 1, real_exam_seed: 2 } as const;
export const SourceType = { real_exam: 0, ai_generated: 1, user_generated: 2 } as const;
export const Visibility = { Private: 0, Shared: 1 } as const;
export const SubmissionStatus = {
  draft: 0, submitted: 1, grading: 2, grading_failed: 3, graded: 4,
  under_dispute: 5, standard_revised: 6, archived: 7, grading_abandoned: 8,
} as const;
export const FollowUpVerdict = { user_correct: 0, user_incorrect: 1, partial: 2, pending: 3 } as const;
export const OverrideScope = { grading_rubric: 0, translation_reference: 1 } as const;
export const OverrideStatus = { observing: 0, active: 1, deprecated: 2 } as const;
export const WeakPointStatus = { active: 0, resolved: 1 } as const;
// 注意：Priority 的 high 是 0，跟直觉的"数字越大越优先"相反——后端自己也在多处代码注释里
// 提醒过这是个"排序时容易踩的坑"（OrderBy 不能写反）。
export const Priority = { high: 0, medium: 1, low: 2 } as const;
export const ScaleType = { band_1_5: 0, score_0_100: 1, rubric_level: 2 } as const;
export const TemplateLayer = { shared_methodology: 0, exam_specific: 1 } as const;
export const CategoryType = { domain: 0, scenario: 1 } as const;
export const SubjectCategory = { translation: 0, language_arts: 1, math: 2, science: 3, other: 4 } as const;
export const CheckpointImportance = { core: 0, peripheral: 1 } as const;
export const MasteryLevel = { New: 0, Familiar: 1, Mastered: 2 } as const;
export const AiOperationType = {
  question_gen: 0, grading: 1, followup: 2, standard_revision: 3, deep_learning: 4, progress_trend: 5,
} as const;

// 反查表 + 展示用中文标签，UI 组件统一从这里取，不要在组件里散落 switch-case
export const SubmissionStatusLabel: Record<number, string> = {
  [SubmissionStatus.draft]: '草稿',
  [SubmissionStatus.submitted]: '已提交',
  [SubmissionStatus.grading]: '批改中',
  [SubmissionStatus.grading_failed]: '批改失败',
  [SubmissionStatus.graded]: '已批改',
  [SubmissionStatus.under_dispute]: '追问中',
  [SubmissionStatus.standard_revised]: '标准已修订',
  [SubmissionStatus.archived]: '已归档',
  [SubmissionStatus.grading_abandoned]: '已放弃批改',
};
// Difficulty / Priority / FollowUpVerdict / OverrideStatus / WeakPointStatus 等同理，
// 每个枚举一张 *Label 表，供组件直接渲染中文，不重复写映射逻辑。
```

### 6.3 请求/响应 DTO（逐个接口核对后端 record 得出，字段名与后端 camelCase JSON 完全一致）

> 完整清单较长，此处按资源列出**关键类型**；每个资源的字段与第 7 节"路由与页面清单"表格里标注的接口一一对应，AI 编码时把对应 Controller 的 `record` 原样翻译成 TS 接口即可（字段顺序、可空性都要照抄，不要"优化"掉看起来多余的字段）。

```ts
// --- Exam Config ---
export interface ExamType {
  id: string; code: string; name: string; subjectCategory: number;
  sourceLanguage: string | null; targetLanguage: string | null; gradeLevel: string | null;
  description: string | null; isActive: boolean; createdAt: string;
}
export interface AssessmentDimension {
  id: string; examTypeId: string; dimensionKey: string; dimensionName: string;
  scaleType: number; passThreshold: string | null; applicableTaskType: number | null;
  levelDescriptions: string; rubricVersion: string;
  effectiveFrom: string; effectiveTo: string | null; sourceReference: string | null; verifiedAt: string | null;
}
export interface ErrorTaxonomy {
  id: string; examTypeId: string; categoryKey: string; categoryName: string;
  description: string | null; exampleCases: string | null;
}

// --- Questions ---
export interface MeaningCheckpoint { id: string; checkpointText: string; checkpointType: string | null; importance: number; }
export interface SeededError {
  id: string; positionStart: number; positionEnd: number; errorTaxonomyId: string;
  errorCategoryKey: string; correctReferenceText: string; note: string | null;
}
export interface QuestionDetail {
  id: string; taskType: number; difficulty: number; title: string; brief: string | null;
  sourceText: string; wordCount: number | null; origin: number; sourceType: number;
  isSeedReference: boolean; inBank: boolean; visibility: number; createdBy: string | null;
  createdAt: string; isActive: boolean;
  meaningCheckpoints: MeaningCheckpoint[];
  taskB: { flawedTranslationText: string; seededErrors: SeededError[] } | null; // 仅 TaskType.B 非 null
  categoryIds: string[];
}
export interface QuestionListItem {
  id: string; taskType: number; difficulty: number; title: string;
  wordCount: number | null; inBank: boolean; createdAt: string;
}
export interface GenerateQuestionRequest {
  examTypeId: string; taskType: number; difficulty?: number | null; categoryId?: string | null;
  seedQuestionIds?: string[] | null; createdBy?: string | null; targetWeakPoints?: boolean;
}

// --- Submissions & Grading ---
export interface CreateSubmissionRequest { questionId: string; userId: string; taskType: number; content: string; }
export interface GradingResultItem {
  id: string; dimensionKey: string; dimensionName: string; rubricVersion: string;
  band: number; passBool: boolean; rationale: string;
  cumulativeDensityFlag: boolean; cumulativeDensityNote: string | null; estimatedPassProbability: number | null;
}
export interface ErrorListItem {
  id: string; positionRef: string | null; sourceTextSnippet: string | null; userTextSnippet: string | null;
  errorCategory: string; dimensionKey: string; impactsCore: boolean;
  explanation: string | null; suggestion: string | null;
}
export interface SubmissionDetail {
  id: string; questionId: string; userId: string; taskType: number; content: string;
  status: number; submittedAt: string | null; createdAt: string;
  gradingResults: GradingResultItem[]; errorList: ErrorListItem[];
}

// --- Follow-ups ---
export interface CreateFollowUpQuestionRequest {
  submissionId: string; userId: string; examTypeId: string; contextRef: string | null; questionText: string;
}
export interface FollowUpQuestionResult {
  id: string; submissionId: string; verdict: number; aiResponse: string;
  submissionStatus: number; standardOverrideId: string | null; standardOverrideStatus: number | null;
}

// --- Deep Learning ---
export interface SentencePattern {
  id: string; patternName: string; exampleSentence: string | null; breakdownSteps: string | null;
  variants: string | null; domain: string | null; scenario: string | null; frequencyTag: string | null;
}
export interface VocabExpression {
  id: string; englishExpr: string; chineseEquiv: string | null; contextNote: string | null;
  category: string | null; domain: string | null; scenario: string | null; frequencyTag: string | null;
}
export interface DeepLearningContent {
  questionId: string; referenceText: string; comparisonNotes: string | null;
  sentencePatterns: SentencePattern[]; vocabExpressions: VocabExpression[];
}
// POST（生成/取缓存）的响应比 GET 多一个 wasCached 字段——两个接口字段不完全相同，
// 不要偷懒共用一个类型导致 POST 场景下 wasCached 被漏掉。
export interface GenerateDeepLearningContentResponse extends DeepLearningContent { wasCached: boolean; }

// --- Weak Points / Progress / Review Library ---
export interface WeakPoint {
  id: string; category: string; description: string | null;
  firstDetectedAt: string; lastSeenAt: string; recurrenceCount: number; status: number; priority: number;
}
export interface ProgressSnapshot {
  id: string; periodStart: string; periodEnd: string; difficultyTier: string | null;
  avgBandMeaningTransfer: number | null; avgBandTextualNorms: number | null; avgBandLanguageProficiency: number | null;
  passRate: number | null; trendNote: string | null; keyTurningPoint: boolean;
}
export interface ReviewPatternItem {
  id: string; questionId: string | null; patternName: string; exampleSentence: string | null;
  domain: string | null; scenario: string | null; frequencyTag: string | null;
  timesEncountered: number; masteryLevel: number; lastReviewedAt: string | null;
}
```

（`periodStart`/`periodEnd` 是后端的 `DateOnly`，JSON 序列化成 `"2026-08-31"` 这种纯日期字符串，不带时间——前端渲染图表 x 轴时直接按字符串排序/`new Date(...)` 都可以，不用担心时区偏移。）

---

## 7. 路由与页面清单（App Router）

> 表格里的"接口"列用 `METHOD path` 简写，完整路径都要加 `/api/v1` 前缀（Server Component 直连）或 `/api/backend` 前缀（Client Component 经代理）。

### 7.1 学生练习端

| 路由 | 页面职责 | 关键接口 | 关键组件 | 备注 |
|---|---|---|---|---|
| `/practice` | 题库浏览：按 taskType/difficulty/categoryId/inBank 筛选 | `GET /questions`, `GET /question-bank-categories` | `question-card.tsx`, `difficulty-badge.tsx` | 无分页，见 3.5 节 |
| `/practice/generate` | AI 出题配置（难度/任务类型/是否命中薄弱点/手动选真题种子） | `POST /questions/generate`, `GET /weak-points`（判断是否有薄弱点可选） | `ai-loading-state.tsx` | 出题是慢请求，见 3.4 节 |
| `/practice/[questionId]` | 答题页：TaskA 显示原文+翻译输入框；TaskB 显示原文+带错误译文+标注工具 | `GET /questions/{id}`, `POST /submissions` | `task-a-editor.tsx` / `task-b-annotator.tsx` + `selectable-source-text.tsx` | 提交后跳转到评分结果页 |
| `/submissions/[submissionId]` | 评分结果：各维度 Band、错误列表、追问入口 | `GET /submissions/{id}`, `POST /submissions/{id}/grade`（若还未评分）, `POST /follow-ups` | `grading-result-panel.tsx`, `dimension-band-row.tsx`, `error-list-item.tsx`, `follow-up-dialog.tsx` | grade 是慢请求；追问对话框提交后展示 AI 回复+是否触发了 standard override |
| `/deep-learning/[questionId]` | 参考译文 + 技巧笔记 + 句型/词汇卡片 | `POST /questions/{id}/deep-learning`（首次生成或取缓存）, `GET /questions/{id}/deep-learning` | `reference-translation-card.tsx`, `sentence-pattern-card.tsx`, `vocab-expression-card.tsx` | `wasCached` 字段判断是否首次生成，见 3.6 节 |
| `/review-library` | 跨题目复习库：句型/词汇按 domain/scenario/frequencyTag 筛选，标记掌握程度 | `GET /review-library/patterns`, `GET /review-library/vocab`, `POST /review-library/patterns/{id}/review`, `POST /review-library/vocab/{id}/review` | 通用列表 + `enum-select.tsx`（MasteryLevel 筛选） | 每条记录带 `questionId`，点击跳回原题（design doc §2.2） |
| `/weak-points` | 薄弱点列表，按状态筛选 | `GET /weak-points` | `weak-point-card.tsx` | 只读，无操作按钮（后端没有手动关闭薄弱点的接口） |
| `/progress` | 学习曲线仪表盘：各维度 Band 趋势、通过率、AI 趋势点评 | `GET /progress?difficultyTier=` | `band-trend-chart.tsx`, `pass-rate-chart.tsx` | 用 Recharts；`trendNote`/`keyTurningPoint` 直接展示为标注文字/高亮点 |
| `/standard-overrides` | 追问引发的评分标准修正记录（审计追溯，只读） | `GET /standard-overrides`, `GET /standard-overrides/{id}` | 简单列表+详情 | 面向"想搞清楚为什么评分标准变了"的场景，非高频页面 |

### 7.2 内容管理后台（`/admin`，见第 16 节的权限说明）

| 路由 | 页面职责 | 关键接口 |
|---|---|---|
| `/admin/exam-types` | 考试类型列表 + 新建 | `GET /exam-types`, `POST /exam-types` |
| `/admin/exam-types/[examTypeId]/dimensions` | 评分维度列表 + 新建版本（第 3.8 节的版本化语义） | `GET/POST /exam-types/{id}/assessment-dimensions` |
| `/admin/exam-types/[examTypeId]/error-taxonomies` | 错误分类列表 + 新建 | `GET/POST /exam-types/{id}/error-taxonomies` |
| `/admin/prompt-templates` | Prompt 模板列表（按 examTypeId/subjectCategory/templateType 筛选）+ 新建 | `GET/POST /prompt-templates` |
| `/admin/llm-providers` | 4 个 AI 供应商的开关/模型切换/thinking-effort 配置 | `GET /llm-provider-settings`, `PATCH /llm-provider-settings/{key}`, `POST /llm-provider-settings/{key}/activate`, `GET/POST /llm-provider-settings/{key}/models`, `POST /llm-provider-settings/{key}/models/{model}/select` | 唯一一个"设置类"页面，不是 CRUD 列表，单独设计（开关组+模型下拉） |
| `/admin/question-bank-categories` | 题库分类树（domain/scenario 两套体系）+ 新建 + 给题目打标签 | `GET/POST /question-bank-categories`, `POST /question-bank-categories/{categoryId}/questions/{questionId}` |
| `/admin/questions/import` | 手工导入题目（含"标记为真题种子" `isSeedReference`） | `POST /questions`（`ImportUserQuestionRequest`） | TaskB 手工导入需要同一个 `selectable-source-text.tsx` 标注种子错误 |

---

## 8. 组件库与复用架构

### 8.1 三层组件模型

1. **`components/ui/`**——shadcn/ui 生成的纯展示基础件（Button/Dialog/Table/Select/Tabs/Card/Badge/Toast...），不含任何业务逻辑，全站唯一的视觉语言来源。
2. **`components/shared/`**——跨领域复用的"胶水组件"：
   - `ai-loading-state.tsx`：接收 `status: 'idle'|'pending'|'success'|'error'`，统一渲染"AI 处理中"提示、错误提示（用 `ApiError.problem` 渲染，503 特殊文案）。
   - `error-banner.tsx`：接收 `ApiError`，渲染标题+correlationId。
   - `enum-select.tsx`：接收一张 `Record<number,string>` 标签表 + 当前值，渲染下拉框，`admin` 和练习端所有"选难度/选任务类型/选状态"的地方都用它，不重复写 `<Select>` + `<SelectItem>` 循环。
3. **`components/<domain>/`**——业务组件，只在对应领域的页面里用，但**同一个业务组件允许被多个页面复用**（如 `grading-result-panel.tsx` 既用在提交后即时展示，也用在历史提交详情页）。

### 8.2 值得重点设计的复用组件

- **`selectable-source-text.tsx`**：核心是把一段文本渲染成可选中的 span 序列，选中后回调 `(positionStart, positionEnd)`。这个组件被三处复用：TaskB 答题标注、TaskB 手工导入题目时标注种子错误、（未来可能的）追问时引用原文片段。设计成受控组件，`highlightRanges` prop 接收已标注的区间用于高亮回显。
- **`crud-table.tsx`**：泛型组件，`columns: ColumnDef<T>[]` + `createFormSchema: ZodSchema` + `onCreate: (values) => Promise<T>`，承载 admin 里 5 个"只有 List+Create"的资源页面，减少重复的表格+新建弹窗代码。
- **`grading-result-panel.tsx`**：接收 `SubmissionDetail`，内部再拆 `dimension-band-row.tsx`（单个维度的 Band+进度条+文字说明）和 `error-list-item.tsx`（单条错误，TaskB 情况下还要能高亮回原文对应位置——复用 `selectable-source-text.tsx` 的高亮态）。

### 8.3 逻辑复用（Hooks）

- `hooks/use-exam-config.ts`：见 9.1 节，全局只请求一次的 ExamType/Dimensions/ErrorTaxonomies。
- `hooks/use-current-user.ts`：包一层 Supabase 的 `useUser`，暴露 `{ userId, email, isLoading }`，所有需要 `userId` 的表单/请求都从这里拿，不允许组件直接调 Supabase client。
- 每个 `lib/api/*.ts` 里的 React Query hooks 本身就是"逻辑复用"的主力——`useSubmission(id)`、`useGenerateQuestion()` 等，组件只管调用和渲染。

---

## 9. 关键业务流程的前端实现细节

### 9.1 ExamTypeId 的全局引导

几乎每个写接口（生成题目、评分、追问、深入学习）都要求传 `examTypeId`，而 MVP 阶段后端**只有一个 exam type**（NAATI CT）。前端不应该在每个页面都重新 `GET /exam-types` 再筛出那一个：

```ts
// hooks/use-exam-config.ts
export function useExamConfig() {
  return useQuery({
    queryKey: ['exam-config'],
    queryFn: async () => {
      const examTypes = await listExamTypes(api, { isActive: true });
      const examType = examTypes[0]; // MVP 只有一个；未来多 exam type 时这里要改成用户选择
      const [dimensions, errorTaxonomies] = await Promise.all([
        getAssessmentDimensions(api, examType.id),
        getErrorTaxonomies(api, examType.id),
      ]);
      return { examType, dimensions, errorTaxonomies };
    },
    staleTime: Infinity, // 配置数据几乎不变，整个会话期间复用
  });
}
```

在根布局（`(main)/layout.tsx`）里预取一次，放进 React Query 缓存，下游任何组件 `useExamConfig()` 都是缓存命中，不重复请求。**如果后端未来真的上线第二个 exam type，这个 hook 是唯一需要改的地方**（改成读用户选择的 exam type，而不是硬取第一个）。

### 9.2 TaskB 标注组件的数据流

```
后端 flawedTranslationText (string)
        │
        ▼
<SelectableSourceText text={flawedTranslationText} highlightRanges={annotations} onSelect={...} />
        │  用户框选一段文字 → 得到 { start, end } 字符偏移
        ▼
弹出小面板：选 errorCategory（从 useExamConfig().errorTaxonomies 里选）+ 填 correctedText
        │
        ▼
annotations: Array<{ positionStart, positionEnd, errorCategory, correctedText }>
        │  提交时
        ▼
POST /submissions  { taskType: 1, content: JSON.stringify(annotations) }
```

关键约束（对照 3.9 节）：`positionStart`/`positionEnd` 必须是原生 JS 字符串索引（`string.length`/`slice` 语义），不能是"第几个词"或经过 Unicode 规范化后的索引——后端 `TaskBSeededError` 用的就是最朴素的 C# 字符串索引，两边字符编码只要都按 UTF-16 code unit 算就是对齐的（中英混排文本要小心 emoji/生僻字的代理对问题，但翻译练习场景文本基本是标准 BMP 字符，风险很低，不用额外处理）。

### 9.3 AI 调用的统一等待态模式

```tsx
function GradeButton({ submissionId, examTypeId }: Props) {
  const { mutate, status, error } = useGradeSubmission(submissionId);
  return (
    <>
      <Button disabled={status === 'pending'} onClick={() => mutate(examTypeId)}>
        {status === 'pending' ? '批改中…' : '提交批改'}
      </Button>
      <AiLoadingState status={status} error={error} pendingHint="AI 正在批改，可能需要几秒到十几秒" />
    </>
  );
}
```

所有 AI 相关按钮（出题/评分/追问/深入学习）都套这个模式，`AiLoadingState` 内部对 `error instanceof ApiError && error.status === 503` 特殊处理成"AI 暂时不可用，请稍后重试"文案，其余错误走通用 `error-banner.tsx`。

### 9.4 提交状态机在 UI 上的映射

后端 `SubmissionStatus` 是一个明确的状态机（design doc §4.1）。前端评分结果页要能正确处理每个状态下的可操作性：

| 状态 | 页面应该展示 |
|---|---|
| `submitted` | "尚未批改"提示 + "开始批改"按钮 |
| `grading` | 批改中（理论上前端很少能观察到这个中间态，因为 grade 接口是同步等到完成才返回；只有网络异常导致轮询/刷新时才可能看到） |
| `grading_failed` | 批改失败提示 + "重新批改"按钮（复用同一个 grade 接口） |
| `graded` | 完整评分结果 + "对某条判定有异议？发起追问"入口 |
| `under_dispute` | 追问处理中（同样通常同步返回，很少能观察到） |
| `standard_revised` / 之后回到 `graded` | 追问判定用户对，展示"该判定已确认修正，评分标准已更新"提示 |
| `archived` | 只读展示，隐藏所有操作按钮 |

---

## 10. 状态管理与数据获取策略

**判断一个组件该是 Server Component 还是 Client Component 的规则**（写死，AI 编码时直接套用，不要每次重新判断）：

1. 默认所有组件都是 Server Component（App Router 默认行为，不用做任何事）。
2. 只有以下情况才加 `"use client"`：
   - 用到 `useState`/`useEffect`/`useContext` 等 React hooks。
   - 用到 React Query 的 `useQuery`/`useMutation`。
   - 有事件处理器（`onClick`/`onChange`/表单提交）。
   - 用到浏览器 API（`window`、`localStorage`）或 Supabase 浏览器端 client。
3. **页面级数据获取优先放在 Server Component**（`page.tsx` 本身是 `async function`，直接 `await` 第 5.3 节的服务端 `apiFetch`），把拿到的数据作为 props 传给下层的 Client Component 去做交互——不要整个页面标 `"use client"` 之后在 `useEffect` 里发首屏请求，那样会多一趟客户端请求瀑布。
4. **表单、按钮点击触发的写操作**必须在 Client Component 里用 React Query 的 `useMutation`（走代理层），因为需要 loading/error 状态和成功后的缓存失效（`invalidateQueries`）。
5. 全局 `QueryClientProvider` 只包一层，放在根 `layout.tsx` 里的一个 `"use client"` Provider 组件中（App Router 标准写法：一个单独的 `providers.tsx` 文件）。

**服务端数据 → 客户端 React Query 缓存的衔接**：对于"Server Component 已经拿到了数据，但下游 Client Component 之后可能需要 refetch/mutate"的场景（比如评分结果页），用 TanStack Query 的 `HydrationBoundary` 把服务端 `prefetchQuery` 的结果序列化传给客户端，客户端 `useQuery` 直接命中缓存不重新请求，但后续 `invalidateQueries` 又能正常工作。

---

## 11. 表单与校验

- React Hook Form 管表单状态，Zod 写 schema，`@hookform/resolvers/zod` 接起来。
- **Zod schema 尽量镜像后端 FluentValidation 规则**（已知的几条，AI 编码时对照这些写 client 端校验，减少无意义的往返请求）：
  - `CreateSubmissionValidator`：`content` 非空且必须是合法 JSON；TaskA 时是 JSON 字符串，TaskB 时是非空数组且每项含 `positionStart`/`positionEnd`(number)/`errorCategory`(非空字符串)/`correctedText`(字符串)。
  - `GenerateQuestionCommand`：`seedQuestionIds` 最多 5 个、不能重复、不能含空 GUID。
  - `ImportUserQuestionValidator`：TaskB 必须有 `flawedTranslationText` 且至少一条 `seededErrors`，且 `positionStart < positionEnd`、区间不重叠、都落在 `flawedTranslationText.length` 以内。
- **前端校验永远是"提前给用户反馈"，不是唯一防线**——后端校验失败时的 400 响应（带 `errors` 字典，见 3.2 节）要能映射回具体表单字段，用 `form.setError(fieldName, { message })` 兜底显示后端的校验信息，避免"前端过了但后端拒绝"时用户一脸懵。

---

## 12. 样式与设计系统

- Tailwind CSS 4（原子类）+ shadcn/ui 的 CSS 变量主题（`--primary`、`--muted` 等），不额外引入 CSS-in-JS。
- 因为是面向中文用户的翻译练习工具，**默认字体栈要包含中文字体**（`font-sans` 配置里加 `"PingFang SC", "Microsoft YaHei"` 等 fallback），Band 分数/进度条等数字类展示可以用等宽数字特性（`font-variant-numeric: tabular-nums`）保证对齐。
- Band 1-5 的视觉编码统一：Band 1（最好）→ 绿色，Band 5（最差）→ 红色，中间线性过渡——`dimension-band-row.tsx` 和 `band-trend-chart.tsx` 共用同一份颜色映射函数（`lib/utils.ts` 里的 `bandToColor(band: number)`），不要两处各写一套配色导致视觉不一致。
- 深色模式：shadcn/ui 默认支持，按需开启（非本方案的核心诉求，做成"开箱即用不用额外设计"即可）。

---

## 13. 测试策略

| 层级 | 工具 | 覆盖对象 |
|---|---|---|
| 单元测试 | Vitest | 纯函数：枚举标签映射、`bandToColor`、字符偏移选区的选中逻辑、Zod schema |
| 组件测试 | Vitest + React Testing Library | 关键交互组件：`selectable-source-text.tsx` 的选区行为、`grading-result-panel.tsx` 的渲染、`crud-table.tsx` 的表单提交 |
| API 层测试 | Vitest + MSW（Mock Service Worker） | `lib/api/*.ts` 的请求构造是否正确（尤其是枚举序号、TaskB content 的 JSON 编码是否符合 3.9 节格式），mock 后端响应而不是真的打后端 |
| E2E | Playwright | 核心链路冒烟测试，直接对应设计文档 §11.2 Step 10 提到的"登录→出题→作答→提交→查看结果"；建议对接一套专门的测试用 Supabase 项目 + 一个跑着真实 .NET 后端（或 Testcontainers 起的）的环境，不要打生产 Supabase |

---

## 14. 环境变量与部署

```bash
# .env.local.example
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxxx            # 公开的 anon key，浏览器可见，Supabase 设计上就是给客户端用的
BACKEND_API_BASE_URL=https://api.your-domain.com   # 服务端专用，不加 NEXT_PUBLIC_ 前缀，绝不能暴露到浏览器
```

- **`BACKEND_API_BASE_URL` 绝对不能加 `NEXT_PUBLIC_` 前缀**——一旦加了就会被打进客户端 bundle，等于告诉所有人后端真实地址，而且完全绕开了第 5 节精心设计的代理层意义（虽然代理层本身也不是安全边界，而是解决 CORS 问题，但暴露内部地址仍然没有任何好处）。
- 部署建议：Vercel（Next.js 官方平台，App Router 特性支持最好）或自托管 Node server 均可；.NET 后端和 Next.js 前端是两个独立部署单元，只要网络互通、`BACKEND_API_BASE_URL` 配对即可，不要求同机部署。
- 和后端共用同一个 Supabase 项目（同一个 `Supabase:ProjectUrl`），否则 JWT 的签发方和后端验证的 `Authority` 对不上，鉴权会全部失败。

---

## 15. 分阶段开发路线图

后端功能已经全部就绪（Step 0-9），所以前端的阶段划分**按用户能拿到的完整体验切**，不是按后端 Step 来对齐：

**Phase 1 —— 打通登录 + 核心练习闭环**
- 登录/会话（第 4 节）、代理层（第 5 节）、类型层（第 6 节）先落地——这是后面所有页面的地基。
- `/practice` 题库浏览、`/practice/[questionId]` 答题（先做 TaskA，TaskB 标注组件更复杂放 Phase 1.5）、`/submissions/[submissionId]` 评分结果展示。
- 验收标准：一个真实用户能登录、挑一道 TaskA 题、提交、看到 AI 批改结果。

**Phase 1.5 —— TaskB 标注 + 追问**
- `selectable-source-text.tsx` 组件、TaskB 答题、追问对话框（`follow-up-dialog.tsx`）。

**Phase 2 —— AI 出题 + 深入学习**
- `/practice/generate` 出题配置页、`/deep-learning/[questionId]`。

**Phase 3 —— 学习分析闭环**
- `/weak-points`、`/progress`（图表）、`/review-library`。这几个是"练完之后回头看"的页面，产品价值上排在"能练"之后。

**Phase 4 —— 管理后台**
- `/admin/*` 六个页面，全部基于 `crud-table.tsx` 复用组件，预计是开发成本最低的一批页面（因为组件已经在 Phase 1-3 沉淀好），放最后做也不影响学生端可用性。`/admin/llm-providers` 因为不是纯 CRUD，单独花时间。

**Phase 5（可选）—— E2E 测试 + 生产加固**
- Playwright 冒烟测试、性能检查、错误监控接入（如 Sentry），与后端 Step 10 的"生产级健壮性收尾"呼应但各自独立推进。

---

## 16. 已知坑与对后端的建议

这些是写这份方案时发现的、值得记录但**不属于前端能单方面解决**的问题：

1. **枚举序列化成数字**（3.3 节）——如果后端愿意在 `Program.cs` 的 `AddControllers()` 后加一行：
   ```csharp
   builder.Services.Configure<Microsoft.AspNetCore.Mvc.JsonOptions>(o =>
       o.JsonSerializerOptions.Converters.Add(new System.Text.Json.Serialization.JsonStringEnumConverter()));
   ```
   所有枚举就会变成 `"medium"` 这样的字符串，前端类型层会直观很多（不用维护第 6.2 节这张序数表，也不怕后端哪天调整枚举成员顺序导致序数错位——**这才是数字契约真正的风险点**：C# 枚举的序号是"声明顺序"，后端如果哪天在中间插入一个新成员而不是加在最后，所有前端硬编码的序数映射会全部错位且不会有任何编译期或运行期报错）。这个改动是否要做、什么时候做，由后端团队决定，前端目前按"数字契约"实现是可行的，只是多一分脆弱性，值得记录在案。
2. **没有 CORS 配置**——本方案用"代理层"绕开了这个问题（第 5 节），不需要后端改动。但如果未来有别的客户端（原生 App、第三方集成）需要直连后端，届时后端需要显式加 `AddCors`。
3. **列表接口无分页**（3.5 节）——数据量小的阶段无影响，题库/复习库数据量上来后需要前后端协调加分页参数，不是前端单方面能补的。
4. **`/admin` 没有真正的权限保护**——后端完全没有角色系统（AGENTS.md 原文："there is no auth/role system anywhere in this codebase to gate it behind"），所以本方案里 `/admin` 路由组的保护**只是前端约定**（比如检查登录用户的邮箱在一个硬编码白名单里），任何人只要拿到一个有效 Supabase 账号、知道 `/admin` 路径，技术上都能绕过前端直接调后端管理接口。如果这个后台要真正上线给非开发者使用，需要后端配合加角色/权限校验，目前只适合"内部人员，信任模型下使用"。
5. **评分维度只能查"当前生效版本"**（3.8 节）——如果后台需要做"查看某个 dimension 历史上所有版本"的审计页面，后端目前没有对应的查询接口，需要新增。
